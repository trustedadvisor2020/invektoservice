# Hot Lessons — Standing Rules

> **Her session yüklenir.** Sadece yüksek-önem + tekrar-eden + ileri-kural. Anlatı YOK. Tam arşiv (~360 satır): `lessons-learned.md` + `lessons-learned-archive.md` — SADECE Grep (codex/deploy/db öncesi ilgili keyword'ü tara). Asla tam Read.

## Codex review (en sık tekrarlayan)
- Shared-Postgres read (tenant_registry/tenant_instances) PR'ında /rev summary'ye BAŞTAN yaz: "single shared Postgres + Outbound zaten okur=precedent + query tenant-scoped + schema=<file:line>" → CQ5/CQ9/CQ11 FP döngüsünü iter0'da kapat.
- `null-forgiving operator (!)` = HARD CQ5 FAIL bu projede; pattern-bind/non-null guard kullan.
- Prior-slice şemasına CRUD yazarken migration diff'te olmaz → CQ11 FAIL: /rev summary'ye migration no + constraint adı + commit attest et.
- `git_diff` arg'ına placeholder/"SEE diff_file_path"/parafraz/ABBREVIATED YAZMA → tüm CQ UNKNOWN→FAIL; EXACT `git diff` çıktısını VERBATIM geç. **MCP `codex_review`: inline `git_diff` PRIMARY; ama "Diff too short or empty (0 chars)" hatası gelirse (büyük inline arg transmit edilmemiş — 2026-06-14 Outbound §7'de oldu) tool `diff_file_path`'i FALLBACK olarak GERÇEKTEN OKUR → diff'i `git diff --cached > arch/plans/diffs/{slug}.diff`'e yaz + `diff_file_path` (absolute) geç → PASS doğrulandı.** (Eski kural "diff_file_path otomatik okumaz" güncellendi; yine de placeholder/abbreviated git_diff = UNKNOWN→FAIL, C2 iter0 precedent.) 52KB diff bile inline dene; tek cohesive feature'ı CHUNK'lama (cross-chunk evidence FP).
- Staged dosyayı edit edince staged kopya GÜNCELLENMEZ → /rev öncesi yeniden `git add`.
- Routing değişikliği auto-escalate LOW→MEDIUM; MEDIUM CoVe ≥3 VQ + Auth/Data/Lifecycle span.
- Codex MEDIUM+ client/integration işinde implementasyon ÖNCESİ `codex_consult(critique)` ile planı strestle.
- Codex "project rule" iddiasını codebase prevalence + review-policy.md ile doğrula, yoksa corrected-premise ile reddet.
- broad-catch→typed-catch sweep'inde `ErrorResponse.Create` satırına DOKUNUYORSAN: generic mesaj ("Internal server error") CQ1/CQ12 iter0 FAIL verir — mesaj ÖNCEDEN var olsa bile, dokunduğun an in-scope. Spesifik+aksiyon-alınabilir mesaja çevir ("Failed to save X due to a database error. Please retry."). DB error code yoksa Shared'a EKLEME → mevcut `INV-DB-001` (DatabaseConnectionFailed) reuse. (AgentAI AI-3 iter0 bundan FAIL.)
- broad-catch sweep HTTP/JSON path'inde: önce JSON navigasyonu DEFENSIVE yap (`ValueKind`+`TryGetProperty`+`GetArrayLength` guard, `content[0].text` zincirini kır) → catch seti sadece IO/serialization tiplerine (`HttpRequestException`/`JsonException`/`NotSupportedException`) iner; `catch(Exception) when(ex is A or B)` YAZMA (L36 broad sayar), ayrı typed blok + paylaşılan private helper kullan. Meşru opsiyonel-adım degradation boundary'sini (fail→fallback, OCE-rethrow+log) `arch/codex-context.md`'ye "optional-step degradation boundary" sanctioned exception olarak kaydet → iter0 PASS (AgentAI AI-4..7 precedent).
- broad-catch/HTTP sweep'inde `Headers.Add(name, value)` runtime-değişken value (config api-key, `Bearer {jwt}`) ile FormatException atar; bu istisna typed-catch setinde (Http/Json/NotSupported/OCE) YOK → graceful-degradation boundary'yi kaçırır (500/propagate). `TryAddWithoutValidation` kullan: well-formed value'da wire-identical, malformed'da throw etmez → intended graceful path korunur. (AgentAI AI-9, Codex PASS iter0.)
- Sanctioned broad-catch'i `arch/codex-context.md`'ye yazarken kuralı `catch(OperationCanceledException){throw;}` SHART koşma: `BackgroundService.ExecuteAsync` worker-loop'unda cancellation `break;`/`return;` etmeli (throw host'u faultlar) → kuralı "throw VEYA worker-loop'ta break/return" diye yaz, yoksa kendi politikanı kendin CQ5 FAIL edersin (Knowledge K-13 iter0 bundan). Ayrı not: codex_review'a verdiğin inline `git_diff`'in TAİL'ini doğrula (son hunk var mı) — kesik diff → eksik dosya hunk'ı VQ "diff'te yok=UNKNOWN/FAIL" false-signal verir (Knowledge iter0'da RetrievalService hunk'ı düşmüştü).
- Hosted/background SWEEP job'unda cross-tenant query (tüm tenantları tara) → CQ9 "tenant_id filter yok = FAIL" verir (summary'deki precedent iddiası TEK BAŞINA yetmez). Fix: sanctioned-exception'ı `arch/codex-context.md`'ye KAYITLA (Tenant Isolation + CQ9: request-path ≠ background sweep; precedent FetchPendingOutboxBatchAsync) → diff'e dahil et → iter1 PASS. Yeni INV kodu eklerken AYNI commit'te `arch/errors.md`'ye yaz (CQ12).
- broad→typed sweep'te `catch(NpgsqlException)` exception-VAR'sız + log'suz = CQ12 FAIL: handled exception'ı TrafficLoggingMiddleware loglamaz (sadece status loglar). Logger inject etmeyen handler'a `[FromServices] JsonLinesLogger jsonLogger` EKLE + `catch(NpgsqlException ex)` + `jsonLogger.SystemError(ex.Message)`. Coded response yetmez, log ŞART. (WAA iter0 bundan FAIL.)
- Sanctioned broad-catch ex.Message'ı SADECE ErrorResponse'tan değil, çağırana DÖNEN her değerden temizle: result objesi (ör. `BulkMineResult.Errors` koleksiyonu) endpoint'ten return ediliyorsa Codex onu da "client'a sızıyor" der → ham mesajı log'a, dönen koleksiyona sanitize-not koy. ErrorResponse.Details temiz olsa bile dönen payload kaçak olabilir. (WAA iter1 bundan FAIL.)
- Multi-subsystem orchestration handler (MSSQL `Microsoft.Data.SqlClient` + PG): `NpgsqlException`-only daralt = `SqlException`'ı bare-500'e kaçırır → audit "DB-path" dese bile sanctioned coded-boundary (WAInsightComputeFailed) KORU, codex-context.md'ye "MSSQL+PG, SqlException ulaşılabilir" gerekçesiyle yaz. Sanctioned broad-catch eklemeleri PER-BRANCH (master'da AgentAI/Knowledge eklemeleri YOK, unmerged) → her batch kendi bölümünü ekler, merge'de çakışır (Q çözer).
- broad→typed sweep'te audit'in önerdiği catch tipi DEAD olabilir → callee'nin GERÇEK throw yüzeyini OKU. Örnek: payment /initiate'te audit `catch(HttpRequestException)` önerdi ama `QnbVPosService.InitiatePayment` PURE string-building (banka auto-submit HTML'i CLIENT browser POST eder, server-side HTTP YOK) → o catch dead olurdu. "HTTP yapıyorsa" diye hedge'lenen audit önerisini callee okumadan ekleme; ekli kalan dead-catch CQ8/CQ4 sinyali verir.
- Public bank/payment-return URL handler'ı (`/payment/callback`; banka 3DPay OkUrl/FailUrl'e USER BROWSER'ı POST eder) = sanctioned "her zaman graceful redirect, asla 500" resilience boundary (webhook always-2xx sınıfı) → broad-catch sweep'te DOKUNMA, narrow edersen beklenmedik tip user return-redirect'i kırar (sıfır kazanç). codex-context.md'ye sanctioned yaz, /rev summary'de "out of scope by design" de. Sweep'te `HttpClient.Timeout` set edip CancellationToken wire ETMEYEN call'da tek erişilebilir OCE = transport timeout → `catch(TaskCanceledException)` caller-cancel yutmaz (refresh'te coded 504, best-effort path'te log-and-continue), /rev'de "no token wired" diye gerekçele.
- Timing-safe secret compare: `CryptographicOperations.FixedTimeEquals(rawA, rawB)` length leak'i KAPATMAZ (farklı uzunlukta erken false + süre left.Length'e bağlı) → comment'te "length-independent" = overclaim CQ8/CQ9 FAIL. Her iki tarafı SHA-256→32-byte digest'e hashle SONRA `FixedTimeEquals(hashA, hashB)`; `if (a is null) return false;` guard'ını AYRI tut (silersen `a ?? ""` empty-secret ile eşleşir = auth regression). Genel: comment ne iddia ederse kod onu DELIVER etmeli.
- DI: `AddHttpClient<T>(timeout)` + `AddSingleton<T>()` çifti = singleton DEFAULT 100s client resolve eder, named-timeout DEAD (worker-wedge). State-tutan servisi singleton tut + timeout uygula: named `AddHttpClient(name,timeout)` + `AddSingleton<T>(sp => ActivatorUtilities.CreateInstance<T>(sp, IHttpClientFactory.CreateClient(name)))`.
- Defensive-parse sweep'te `ValueKind == JsonValueKind.Number` guard'ı `GetInt32()`/`GetDouble()`'dan ÖNCE TEK BAŞINA YETMEZ → Codex CQ12 FAIL (Automation §7-B iter0 bundan): Number ama non-integer (`5.5`) veya Int32-aşan değer `GetInt32()`'de hâlâ atar. `ValueKind == Number && el.TryGetInt32(out var v)` (veya `TryGetDouble`) kullan; ValueKind kontrolü TryGetInt32'den önce SHART (TryGetInt32 non-Number'da InvalidOperationException atar). `GetInt32`-on-COUNT(*) ayrı sınıf (SQL `::int` cast, L41).
- Yeni `INV-XX-NNN` const eklerken next-free numarayı ErrorCodes.cs'in max'ından DEĞİL, **errors.md'nin TAM aralığından** al: kodlar errors.md'de REZERVE olup ErrorCodes.cs'de const'ı OLMAYABİLİR (ör. INV-WC-013..025 INMA-channel errors.md'de tanımlı, ErrorCodes.cs const'ı yok → ErrorCodes.cs yanıltıcı max=012 gösterir). `grep -oE 'INV-WC-[0-9]+' arch/errors.md` ile gerçek max'ı bul +1 al. Yoksa C6/C7-sınıfı **değer-collision** (build PASS verir — iki const adı aynı string'i tutar, derleyici görmez; prod'da yanlış-koda map). WebChat §7'de ilk 013 collision'ı diff-review'da yakalandı→026.

## Deploy & prod-truth
- "DONE/committed ≠ deployed": roadmap'te DONE migration prod'a HİÇ uygulanmamış olabilir → multi-paket deploy ÖNCESİ prod DB pre-flight (2x keşif).
- Migration pre-flight `pg_constraint`'e bakıyorsa, migration UNIQUE/partial INDEX ise göremez → `pg_indexes` da kontrol et.
- Prod secret-bearing `appsettings.Production.json`'a section eklerken full ConvertFrom/ConvertTo reserialize ETME → targeted-insert + .bak; boş `current\`'a recover deploy "config korunur" demez.
- NSSM "Running" ≠ healthy + değişmeyen Backend.dll latent regression gizler → port-listen + `/health` curl + canlı asset-hash doğrula; prod-down sorusunda MCP server-status'a değil `nssm status`'a güven.
- server-deploy MCP enum tüm servisleri içermez (VoiceRuntime/VoiceAI eksik) → yanlış dizine deploy riski; yeni servis DLL'i Shared.dll ile PAIR deploy (TypeLoadException guard).
- İki paralel Claude session aynı repo'da deploy edebilir → `git fetch`/working-tree + bundle-hash re-verify.

## DB & concurrency
- Catch `NpgsqlException` (base), sadece `PostgresException` değil → TÜM DB hatasını INV koduna map et.
- `tx.CommitAsync()` NpgsqlException'ı "rolled back, retry-safe" map etmek OVERCLAIM; commit sonrası fresh-conn re-query başarısızlığı ayrı failure mode.
- Junction-insert NON-locking pre-check (SELECT COUNT→INSERT) soft-delete race açar → FOR SHARE row-lock parent write ÖNCESİ.
- Snapshot-tabanlı confirm/dispatch'te parent-state (proje archived/HSM) confirm-zamanı atomik re-validate + claim.
- Yeni DB tablosu = ZORUNLU `GRANT ALL ON {table} TO invekto`; COUNT(*) bigint → `GetInt32` InvalidCastException, `::int` cast veya `GetInt64`.
- Delivery counter FUNNEL (+1) değil PARTITION (yeni bucket +1, canlı eski −1); webhook ack "her zaman 2xx" + cross-service forward typed-catch.
- Monotonic/ordering guard'da ordering-key (occurredAt/version) NULL olabiliyorsa: NULL event newer state'i EZMESİN + anchor'ı NULL'a RESETLEMESİN. `SET ts=COALESCE(@k, ts)` + WHERE `(@k IS NOT NULL AND (ts IS NULL OR ts<=@k)) OR (@k IS NULL AND ts IS NULL)` (NULL = only-fill-if-empty, lowest priority). Naif `@k IS NULL OR ts<=@k` = stale-overwrite + anchor reset → CQ9 FAIL (C2 iter2 bundan).

## Silent-failure / audit
- Silent success > silent reject TEHLİKELİ; "specific error code döndürüyor" ≠ "silent değil" — audit-table contract varsa HER reddedilen deneme AUDIT-BEFORE-SERVE throwing-insert.
- Claimed job ASLA stranded kalmasın (HER exception typed-catch+finally); broad `catch(Exception)` YASAK (Codex `when(filter)` ile bile broad sayar), typed INV response.

## Partner / INMA (overclaim guard)
- Partner "mekanizma var/aktif" cevabı ≠ blocker ÇÖZÜLDÜ → cevabı gate'in LİTERAL koşuluna + kanıta map et, "unblock" overclaim YAPMA (3x); "webhook" deyince yeni endpoint tasarlamadan ÖNCE mevcut per-tenant webhook wiring'i kontrol et.

## Tooling (PowerShell/Bash)
- Bash tool'da commit mesajı için PowerShell here-string `@'...'@` KULLANMA → subject'e `@ ` sızar; `Out-File -Encoding utf8` git-diff redirect'i mojibake → Codex'e ASLA PowerShell redirect diff verme; `$ProgressPreference='SilentlyContinue'` ile server-exec CLIXML stderr şişmesini önle.
- POST-body prod smoke'unda (server-exec PowerShell) `curl.exe -d '{"k":"v"}'` → PowerShell native-arg passing embedded `"`'leri STRIP eder → curl invalid JSON gönderir → endpoint JsonException→**400** döner = YANILTICI (gerçek 401/200'ü maskeler, "deploy bozuk" paniği). Fix: JSON'u `… | Out-File -Encoding ascii -NoNewline $f` ile dosyaya yaz + `curl.exe --data-binary "@$f"`. (WebChat-6 deploy smoke'unda login validJSON 400 sandım → file-based ile 401 INV-WC-007 çıktı = fix doğru.)
- Git guardrail hook'u `git commit -F ... && git push`'u `push -f` sanıp BLOCK eder (false-positive: aynı satırda "push" + "-F"/"-f") → commit ve push'u AYRI Bash call'a böl.

<!-- hot-lessons sonu -->
