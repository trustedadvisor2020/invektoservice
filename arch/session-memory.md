# Session Memory — Current State

> **BOUNDED FILE.** OVERWRITE edilir (append değil), ≤300 satır. Session init `limit=320` ile okur; sonundaki `END_CURRENT_STATE` işareti o pencerede görünmüyorsa DUR → `/optimize-memory`. Eski tam log: [session-memory-archive.md](session-memory-archive.md) (SADECE Grep). Kalıcı kararlar: [docs/decisions.md](docs/decisions.md). Standing kurallar: [hot-lessons.md](hot-lessons.md).
> **Pilot Mode:** queue+tracking otoritesi `tracking/pilot-launch-roadmap.md`'de; bu dosya son durum detayı.

_Güncel: 2026-06-24 — **REBRAND Invekto → Chatinbox AI — TRACK A FAZ A1-A4 = DONE — Codex /rev PASS (A1-A3 `d33a4c42` iter1; A4 hook-regex+build-cmd `b75a21cb` iter0; wrap-docs `d604d22a`) (branch `rebrand/chatinbox`, ⚠️ MASTER'A MERGE EDİLMEDİ — bilinçli, hedef YENİ repo)** (Q: "ürünün adı değişiyor, Invekto hiçbir yerde geçmeyecek; yeni klasör+repo ChatinboxAI.git; içerik/özellik/parolalar aynı"). **Q kararları (kilitli):** FULL-depth (runtime kimlikleri dahil) + production cutover bu hafta + SADECE bu repo + ns kökü `Chatinbox`/DB-issuer `chatinbox` + domain **chatinbox.net** + git **history ALMA** (fresh init; eski invektoservice repo arşiv). **Plan+audit (8 paralel ajan+completeness critic)+Codex consult = `tracking/rebrand-chatinbox-migration.md`** (Track A=kod/repo düşük-risk; Track B=prod cutover gated. Codex düzeltmeleri M1: DB rename ana-cutover'dan çıktı→Faz B5 [MD5 footgun + tüm-bağlantı-kapatma]; M2: NSSM paralel-install [eskiyi silmeden]; M3: JWT semantiği cutover'da DONUK→Faz B6 [case-sensitive issuer 401]). **A1-A3 yapıldı:** git mv 14 src+3 test+sln+postman, dot-anchored case-sensitive `Invekto.`→`Chatinbox.` (851 dosya), build 0-error, **deterministik pure-rename ispatı transform(removed)==added 1916/1916** (tek blank-line artifact), residual=0. **KORUNDU (must-not-touch, doğrulandı):** `/api/invekto/welcome` (partner login), `invekto-` wire prefix (loop-suppression), `"InvektoServis"` JWT literal, lowercase invekto DB/domain (Track B). **EN TEHLİKELİ landmine (KODDAN doğrulandı):** `CxapiWebhookReconcileJob` PublicBaseUrl'den canlı partner (cxapi/WapCRM) webhook'larını otomatik SET eder → domain değişince `OwnedWebhookHosts`=[services.invekto.com, yeni-host] ÖNCE set EDİLMELİ yoksa eski URL "foreign" sayılıp stale kalır → ack ölür (prod `CxapiWebhookReconcile.Enabled=true`, P0-3 gate). **KALAN:** A4 ✅DONE (b75a21cb, hook regex+build-cmd, Codex PASS iter0), A5 (frontend wordmark/title/copy + postman download literal [TenantsPage/DashboardPage.tsx] + Vite SPA rebuild), A6 (fresh-init ChatinboxAI.git push — Q ONAYI ŞART, dışa-dönük), Track B (chatinbox.net DNS + `*.chatinbox.net` wildcard cert + `destek@chatinbox.net` mailbox = Q AKSİYONU, sonra gated cutover; "bu hafta ikisi paralel çalışacak, müşteri kopmasın" = dual-domain pencere).**_

_Önceki: 2026-06-18 — **PROJELER + PROJE RAPORU "CANLI İZLEME" AHA = DONE + Codex /rev PASS iter3 + DEPLOYED** (Q "aha çalıştır" → AHA modu #1/#2/#4/#5 seçti → "yap" → "wrap deploy, diğer session değişiklikleriyle birlikte"). Dört iyileştirme: **#1 auto-refresh** — aktif run'da (running/paused) tablo + Rapor drawer 8sn SESSİZ yenileme (spinner YOK, transient hatada son tablo korunur; `loadProjects(silent)` modu; `document.visibilityState='visible'` gate + drawer açıkken tablo poll'u durur [drawer kendi poll'unu yapar, yalnız seçili run `queued>0` iken]). **#2 ilerleme çubuğu** — tablo Durum hücresinde processed/(processed+queued); yeni `ProjectSummary.queued_count` = SUM `outbound_broadcasts.queued` COMPUTED subquery (iki summary SELECT + MapSummary idx 26), **migration YOK**; drain'de %100, paused=amber. **#4 başarısızlık kırılımı** — `GET /api/v1/projects/{id}/report/failure-breakdown`: failed/ambiguous'u Meta-koduna göre gruplar (`substring(ErrText FROM '\((\d+)\)')` GROUP BY, `min(errtext)` örnek [iter2'de array_agg'den değiştirildi], LIMIT 50), `ProjectFailureBucketDto{code,sample_error,count}`, SPA `resolveWaError(sample_error)` etiket; kovaya tıkla → `failCode` filtresi recipients query'ye (BOTH branch `m.status IN ('failed','ambiguous')`-restricted, breakdown predikatıyla birebir; status-chip ile mutually-exclusive). **#5 Esc-to-close** — en-üst katmanı kapatır (bulk-confirm>cancel>send>drawer>edit; pinli WaErrorPopover `aria-label^='WhatsApp hata'` guard → Esc kartı kapatır). Ortak `ErrTextSql`/`ErrCodeSql` sabiti (filter+grouping drift-free). **Codex /rev iter0→3 PASS** (iter0 2 GERÇEK bug yakaladı: CQ9 failCode breakdown'la tutarsız [→both-branch status restrict] + CQ2 logsuz catch [→console.warn]; iter2 CQ6 array_agg buffering [→min]; iter3 12/12 CQ + CoVe). **⚠️ PARALEL-SESSION INCIDENT (tekrar):** iş ortasında başka session HEAD'i `a7f2780a`'ya taşıdı (Faz 2.1 phone-history-names + Export Manager) ve PAYLAŞILAN dosyalarda (api.ts/ProjectsPage.tsx) duran aha FRONTEND'imi (progress bar/auto-refresh/Esc/breakdown UI) kendi commit'ine SÜPÜRDÜ; backend+fix'lerim uncommitted kaldı → ayrı commit `d642d8f4` (backend+fix) + `868cc430` (SPA rebuild) ile reconcile. **DEPLOY (Q talebi: a7f2780a Faz2.1+Export DAHİL):** Outbound+Backend, migration YOK, ikisi HEALTHY; SPA `ProjectsPage-DNDUcnnj.js` canlı (publish öncesi `deploy_output/Backend` stale-asset temizliği — eski 4 bundle birikmişti). Smoke: HEALTHY + auth-gate 401 + temiz startup (route registration hatası yok). **Fonksiyonel UI doğrulama (breakdown/progress/auto-refresh canlı görme) authenticated session gerektirir — Q tarayıcıda bakacak.** **GÜNCELLEME (aynı gün): Q "#4 gerek yok" → BAŞARISIZLIK-NEDENLERİ bölümü TAM kaldırıldı (UI + Outbound endpoint/service/repo + Backend proxy + failCode plumbing + ProjectFailureBucketDto + ErrTextSql/ErrCodeSql); ayrıca satır "Tekrar Gönder" düğmesi İKON-ONLY (Q "yazıyı kaldır"). #1 auto-refresh / #2 ilerleme çubuğu / #5 Esc KORUNDU; recipients query pre-#4 şekline döndü. Commits `a63754e5`(kaldırma)+`c82c389f`(SPA), Codex /rev PASS iter1 (tek blocker risk-label LOW→MEDIUM düzeltmesiydi, kod kusuru değil), Outbound+Backend redeploy HEALTHY, bundle `ProjectsPage-B3BBs5gj.js` canlı.**_

_Önceki: 2026-06-18 — **FEAT-OBI FAZ 2.1 "GEÇMİŞ TABLOSU İSİMLENDİRME" = DONE + DEPLOYED (a7f2780a; bu deploy'da birlikte prod'a çıktı)** (Q: "kampanyanın adını yaz, ID'ye gerek yok; mesaj olarak gönderilen şablonun adını yaz, hover ile zengin popup preview"). Telefon Numarası Ara tablosu: **Kampanya**=proje adı (`projects.name` via `bulk_send_jobs.project_id` LEFT JOIN; ekranda proje yoksa `—`, CSV/PDF'te `name ?? campaign_id` audit-izlenebilirlik için bilinçli ayrışma), **Şablon**=WAPCRM onaylı şablon ADI, **Mesaj**=şablon gövde önizleme + hover'da zengin WhatsApp-stili popup (header/body/footer/buttons, portal). Şablon adı/preview **SPA-side** mevcut `getWaTemplates(instanceId)` ile çözülür (`om.template_ref`→`WaTemplate.templateId` eşleşmesi; per-instance cache + non-blocking fetch + `console.warn` fallback → export yoluna cxapi bağımlılığı YOK). Backend SQL'e `projects` LEFT JOIN + `om.template_ref`/`om.instance_id` select (hepsi tenant_id-scoped, READ-only); DTO+TS 3 additive nullable alan (campaign_name/wa_template_id/instance_id); **şema değişikliği YOK** (kolonlar zaten var → migration yok). DRY: `WaTemplatePreview` ProjectsPage'den ortak `components/WaTemplatePreview.tsx`e taşındı (iki sayfa tek kaynak). Build PASS (.NET 0 + SPA tsc/vite 0). **Codex /rev PASS iter1** (iter0 FAIL: 1 gerçek [CQ2 logsuz catch → `console.warn` eklendi] + 2 evidence-gap [CQ11/Q2 schema+HSM-insert kontratı diff'te yoktu → diff'e read-only EVIDENCE eki → iter1 12/12 CQ + 4/4 CoVe]). Q kararları: kolon=Şablon-ad/Mesaj-önizleme, kampanya fallback='—', şablon adı/preview=ekran-only. **DEPLOY YAPILMADI** (SPA bundle + Backend/Outbound publish gerekir). **PARALEL-SESSION:** `Program.cs` failure-breakdown proxy route'u başka session'ın işi → commit'e DAHİL EDİLMEDİ (selective add), working tree'de bırakıldı, Q'ya flag'lendi._

_Önceki: 2026-06-17 — **FEAT-OBI FAZ 2 "TELEFON NUMARASI ARA" = DONE+DEPLOYED+SMOKED** (Pilot queue; Q "obi faz2 yi planla"→"başla"→"deploy wrap"). Export Manager'a tek-numara geçmişi bölümü: operatör numara girer → o numaraya BU tenant'tan giden TÜM `outbound_messages` (bulk+proje+legacy) zaman-sıralı (tarih·şablon·kampanya·durum·WA hata kodu [WaErrorPopover reuse]·mesaj gövdesi) + CSV/PDF indir. Read-only, tenant yalnız JWT'den (om + LATERAL bulk_send_jobs + outbound_templates hepsi tenant-scoped), INMA YOK, KVKK audit (`export_logs` type `phone_history`, source_name **maskeli last-4** — tam telefon DEĞİL). **Mimari:** `ExportRepository.ReadPhoneHistoryAsync` static SQL `LIMIT @lim::int` (NULL=ALL, concat yok), IsDBNull-guard'lı, deterministik LATERAL; `ExportService` 3 metot (screen cap=1000 + truncation marker, **view = audit YOK**; CSV server_stream + PDF client_render **audit-before-serve**); 2 **POST** endpoint (telefon=PII → querystring YOK) + Backend `OutboundClient.ProxyStreamPostAsync`/`OutboundProxyStreamPost`. Migration **067** additive index `idx_outbound_messages_tenant_phone_created` (tenant_id, recipient_phone, created_at DESC, id DESC) + `export_logs` CHECK +`phone_history`. SPA: ExportManagerPage yeni bölüm + jsPDF lazy import + TR→ASCII translit. **GR0 prod sampling**: stored recipient_phone %100 `+`E.164 = PhoneNormalizer çıktısı → exact-match doğru. Commit **5761ebbd**, pre-impl codex_consult (22 bulgu) + **Codex /rev PASS iter1** (iter0 2 GERÇEK bulgu yakaladı: SQL string-concat [CQ5] + audit'e tam-telefon PII yazımı [CQ9] → ikisi düzeltildi; 12/12 CQ + 3/3 CoVe). Migration prod + Outbound+Backend deploy **10/10 HEALTHY**. **SMOKE (DB-layer, HTTP JWT gerektirir):** Medipol `+905380740566` → 6 satır newest-first ✓; cross-tenant (5050 aynı numara) `leaked_rows=0` ✓; EXPLAIN `Bitmap Index Scan on idx_outbound_messages_tenant_phone_created` ✓._

_Daha önce: 2026-06-17 — **DASHBOARD REFRESH-STAY-ON-ROUTE = DONE+DEPLOYED** (Q: "hangi sayfada refresh yaptıysam orada kalayım, her refresh ana sayfaya gidiyor"). Kök neden: INMA portal F5'lenince iframe'i varsayılan src (`/app/`=ana sayfa) ile yeniden oluşturuyor → SPA ic route'u (örn /projects) kayboluyor. **INMA-side iframe-reload davranışı** — bizim `useAuth` navigate('/') DEĞİL (o yalnız URL-token SSO path'inde, embedded mode'da temiz URL → tetiklenmez). INMA'ya dokunmadan **client-side fix**: `App.tsx`'e `RouteRestore` bileşeni — gezilen route'u `localStorage`'a yazar (login haric, yalnız authenticated), boot kökte (`/`) ise + kayıtlı derin route varsa `replace` ile döner. **localStorage** seçildi (sessionStorage iframe recreation'da temizlenir; localStorage origin-bazlı kalıcı → recreation'ı aşar). `savedPath` `useState` lazy-init ile render-öncesi senkron yakalanır (persist effect boot'ta `/` yazıp ezemeden). One-shot (`restoredRef`), auth-guard → regresyonsuz (unauth → ProtectedRoute zaten /login). **Bilinen edge:** aynı tarayıcı 2 INMA sekmesi farklı sayfadaysa tek localStorage anahtarı son-yazılana döner (tek-sekme normunda sorun yok). Commit **53866535**, **Codex LOW iter0 PASS** (12/12 CQ + 4/4 CoVe, 0 blocker). Deploy = SPA build (`Dashboard/npm run build`→`wwwroot/app`) + zip + **backup-swap extract** (`app`→`app_bak_20260617-192056` rollback) → Backend :5000 health OK, **zero-downtime static swap** (dotnet publish/restart YOK), SPA `index-BAQ7-Wsw.js` canlı. **Test:** INMA'da alt sayfaya git, F5 → orada kalmalı (gerekirse 1 kez Ctrl+F5, index.html no-cache)._

_Daha eski: 2026-06-17 — **WHATSAPP HATA-KODU AÇIKLAMA POPOVER = DONE+DEPLOYED** (Q "131049/130472 ne demek" diye sordu → web research → kalıcı UI özelliğine dönüştü). Projeler raporu 'Hata' kolonu + test-gönderim banner'ında Meta hata koduna **tıkla-sabitle zengin Türkçe popover**: severity rozeti (Tekrar denenebilir/Kalıcı/Yapılandırma/Geçici) + "Neden geldi?" + "Ne yapmalı?" + ham sağlayıcı metni + Meta doc link. **Frontend-only** — backend/DB/şema YOK; kod zaten `r.error` içinde `(NNNNN)` olarak geliyor (ProjectsService.CleanNotSentReason o satırı tutar), client-side çıkarılıp kataloğa bağlanır. 3 dosya: `lib/wa-error-codes.ts` (~60 kod TR katalog; 4 kaynak çapraz-doğrulandı: Meta resmî doc+360dialog+Heltar+msg91; range-gated `extractWaErrorCode` yalnız mesaj-teslimat kod uzayı [80007/130xxx-139xxx/23xxxxx] + stray-sayı reddi; `resolveWaError`+kategori-jenerik fallback) + `components/WaErrorPopover.tsx` (responsive+viewport-safe pin popover; X/Esc/dış-tık/scroll kapanma; a11y focus; kodsuz-metin degradasyonu) + `ProjectsPage.tsx` (kolon+banner bağlama, testResult.reason additive). Commit `9773cae0`, **Codex MEDIUM iter3 PASS** (12/12 CQ+4/4 VQ; iter0-2 FAIL'leri 2 GERÇEK bulgu yakaladı: extraction stray-sayı false-positive + popover viewport-taşma → ikisi de düzeltildi). Deploy = SPA build (Dashboard/`npm run build`→`wwwroot/app`) + zip + **backup-swap extract** (`app`→`app_bak_20260617-190403` rollback) → Backend health OK, **zero-downtime static swap** (dotnet publish/restart YOK), SPA `index-ChYJhBr5.js` canlı._

## Recently Completed (max 10)

- **rebrand-chatinbox-trackA** (2026-06-24) — Rebrand Invekto→Chatinbox AI Track A Faz A1-A4 — A1-A3: git mv 14 src+3 test+sln+postman, dot-anchored case-sensitive `Invekto.`→`Chatinbox.` (851 dosya), build 0-error, deterministik pure-rename ispatı transform(removed)==added 1916/1916, residual=0. KORUNDU: /api/invekto/welcome, invekto- wire prefix, "InvektoServis" JWT literal, lowercase invekto (Track B). Commit `d33a4c42` branch `rebrand/chatinbox`, **MASTER'A MERGE YOK** (hedef yeni ChatinboxAI.git fresh-init, history alma). Codex /rev PASS iter1 (iter0 FAIL = pathspec rename-pairing artifact → pre-existing kod review edilmişti, false-positive). Domain=chatinbox.net. **A4 DONE** (`b75a21cb`, Codex PASS iter0): hook regex [invariant-check/build-reminder/check-shared-microservice/dotnet-check.sh `Invekto\.`→`Chatinbox\.`] + PROJECT_CONFIG build-cmd→`Chatinbox.sln`/`Chatinbox.{SERVICE}.csproj`; KORUNAN INV-/klasör-yolu/Track-B-prod/MCP-ID. Kalan: A5/A6 + Track B. Plan: `tracking/rebrand-chatinbox-migration.md`.
- **projeler-rapor-canli-izleme** (2026-06-18) — Projeler + Proje Raporu AHA #1/#2/#4/#5: #1 aktif-run 8sn sessiz auto-refresh (tablo+drawer, visibility+queued gate, `loadProjects(silent)`); #2 ilerleme çubuğu (`ProjectSummary.queued_count` computed subquery, migration YOK); #4 başarısızlık-kırılımı endpoint (Meta-kodu GROUP BY, min(errtext), `failCode` tıkla-filtrele, ortak ErrCodeSql); #5 Esc-to-close. Codex /rev PASS iter3 (iter0 CQ9 failCode-tutarsızlık + CQ2 logsuz-catch; iter2 CQ6 array_agg → ikisi/üçü düzeltildi). Commits `d642d8f4`(backend+fix)+`868cc430`(SPA). DEPLOYED Outbound+Backend HEALTHY (a7f2780a Faz2.1+Export ile birlikte, migration YOK). **PARALEL-SESSION:** aha frontend'im a7f2780a'ya süpürüldü, backend ayrı commit ile reconcile. UI fonksiyonel doğrulama Q'da. **SONRA (aynı gün): #4 başarısızlık-nedenleri TAM kaldırıldı (Q "gerek yok", `a63754e5`+`c82c389f`) + satır resend ikon-only; #1/#2/#5 kaldı; Outbound+Backend redeploy HEALTHY.**
- **obi-phone-history-names** (2026-06-18) — Telefon Numarası Ara tablosu isimlendirme: Kampanya=proje adı (`projects.name` via `bulk_send_jobs.project_id`; ekran proje yoksa `—`, CSV/PDF `name ?? campaign_id`), Şablon=WAPCRM şablon adı, Mesaj=gövde önizleme + hover zengin popup. Şablon adı/preview SPA-side `getWaTemplates` ile çözülür (`om.template_ref`→`templateId`, per-instance non-blocking cache + console.warn fallback). Backend SQL +projects LEFT JOIN + `om.template_ref`/`om.instance_id` (tenant-scoped, READ-only); DTO 3 additive nullable alan; **migration YOK** (kolonlar mevcut). DRY: `WaTemplatePreview` ortak `components/`e taşındı. **Codex /rev PASS iter1** (iter0: CQ2 logsuz catch [→console.warn] + CQ11/Q2 evidence-gap [→diff'e read-only schema/contract eki]). Q: kolon=Şablon-ad/Mesaj-önizleme, fallback='—', export=ekran-only. **DEPLOY BEKLİYOR.** Program.cs failure-breakdown = paralel session, commit'e alınmadı.
- **projeler-report-drawer-sort+polish** (2026-06-17, PARALEL SESSION işi — ben retroaktif review+reconcile ettim) — Rapor drawer: sortable kolonlar (whitelist ORDER BY `sort/dir` → ProjectsRepository/Service/endpoint/api.ts, NULLS LAST + id DESC tiebreak, injection-safe) + "Durumu Yenile" pull-ÖNCE-sonra-refetch fix + bulk-resend ikon+rozet + tıklanabilir özet-chip durum filtreleri. **INCIDENT:** bu iş paylaşılan working tree'de staged'di; benim bare `git commit`'im (wrap doc'u için) onu süpürdü (`498e22d6`) + prod'a deploy etti → unreviewed indi. Damage-control: staged kalanı pathspec'le commit (`2244e45c`) + **retroaktif Codex /rev PASS iter1** (12/12 CQ+3/3 CoVe, kod sağlam) + SPA clean-rebuild+redeploy → **prod=master, Backend HEALTHY**. Lesson kaydedildi (bare-commit shared-index hazard).
- **feat-obi-phone-history** (2026-06-17) — FEAT-OBI Faz 2 "Telefon Numarası Ara": Export Manager'da numara → o numaraya bizden giden TÜM `outbound_messages` geçmişi (şablon/kampanya/durum/WA hata kodu/mesaj gövdesi) + CSV/PDF. Read-only, tenant JWT-only, KVKK audit (export_logs phone_history, maskeli last-4). Migration 067 (additive index + CHECK), 2 POST endpoint (PII→no-querystring), SPA bölüm + jsPDF lazy. GR0 prod-sampling (phone format=normalizer çıktısı) + Codex /rev PASS iter1 (iter0 SQL-concat + tam-telefon-PII-audit blocker'larını yakaladı). Commit `5761ebbd`, Outbound+Backend deploy 10/10 HEALTHY, smoke: 6 satır + cross-tenant leaked=0 + index-scan doğrulandı.
- **dashboard-refresh-stay-on-route** (2026-06-17) — INMA iframe içine gömülü Dashboard SPA'da parent F5 → iframe varsayılan src (`/app/`) ile yeniden oluşup ic route kayboluyordu (ana sayfaya atıyordu). Client-side fix: `App.tsx` `RouteRestore` bileşeni — son route'u **localStorage**'a yazar (sessionStorage iframe recreation'da silinir), boot kökteyse + kayıtlı derin route varsa `replace` ile döner. Auth-guard + one-shot + lazy-init senkron capture → regresyonsuz. Commit `53866535`, Codex LOW iter0 PASS (12/12 CQ+4/4 CoVe). Deploy zero-downtime SPA static swap (`index-BAQ7-Wsw.js`, rollback app_bak_20260617-192056). Edge: 2 sekme farklı sayfa → tek anahtar son-yazılana döner.
- **outbound-campaign-template_id-null-500** (2026-06-17) — `ReadCampaignResponse` nullable `outbound_campaigns.template_id`'i guard'sız `GetInt32(3)` okuyordu → `GetCampaignAsync`/`ListCampaignsAsync` HTTP 500. Fix `IsDBNull(3)?0:GetInt32(3)` (0 sentinel, Shared contract intact). Commit `72862ae4`, Codex iter0 PASS (12/12 CQ+3/3 CoVe), Outbound deploy HEALTHY binary-fresh.
- **projeler-wa-error-popover** (2026-06-17) — Projeler raporu 'Hata' kolonu + test-send banner'ında WhatsApp/Meta hata koduna tıkla-sabitle zengin Türkçe popover (severity rozeti + neden + ne yapmalı + ham metin + Meta doc link). Frontend-only: `lib/wa-error-codes.ts` ~60 kod TR katalog (4 kaynak doğrulandı) + range-gated extract + `components/WaErrorPopover.tsx` (responsive/viewport-safe/a11y) + ProjectsPage bağlama. Commit `9773cae0`, Codex MEDIUM iter3 PASS (iter0-2 extraction stray-sayı + popover taşma bulgularını yakaladı, düzeltildi). Deploy zero-downtime SPA static swap (`index-ChYJhBr5.js`, rollback app_bak_20260617-190403).
- **projeler-report-drawer-ux** (2026-06-17) — Rapor drawer UX: tam-ekran (sağ panel→full viewport) + **sunucu-taraflı Durum dropdown filtresi** (tüm sayfalar, `m.status = ANY(string_to_array(@status,','))` count+page, value-bound, tenant-scope; 'sending,posting'→Gönderiliyor + paused dahil) + 2-dijit yıl (`fmtDateTime`) + **Numara kolonu freeze** (sticky left-0 + group-hover). Commit `07be65b0`, Codex MEDIUM iter1 PASS (12/12 CQ + 4/4 CoVe), Outbound+Backend deploy HEALTHY. 6-boyut adversarial verify (0 blocker, 2 warn fix'li). **PARALLEL-SESSION NOTU:** `git add <file>` diğer session'ın commit'siz resend değişikliğini staging'e karıştırdı → Codex CQ3 yakaladı → revert-stage-commit-reapply ile İZOLE ettim (deploy temiz kaynaktan), onlar `130d4e85` olarak commit etti.
- **projeler-resend-503-hotfix** (2026-06-17) — Rapor "Yeniden gönder" HER denemede 503 (Q canlı console paylaştı). Kök: resend SQL `completed` broadcast'ı `status='sending'` ile reopen ediyordu ama `chk_broadcast_status` yalnız queued|processing|completed|failed kabul eder ('sending'=outbound_MESSAGES sözlüğü) → PG **23514** → ProjectDbError → 503. Tüm 31 broadcast 'completed' → resend %100 kırık (tek+bulk). Fix `'sending'`→`'queued'` (RequeueForResend + RequeueAllForResend). Commit **130d4e85**, Codex iter0 PASS, Outbound deploy HEALTHY, post-restart 0 fail. Gerçek hata `C:\Invekto\Outbound\logs\{date}.jsonl`'de (MCP stdout/stderr default path boş görünüyordu). Lesson kaydedildi.
## Audit kalanı (henüz dokunulmamış — opsiyonel sonraki iş)

- **Automation:** Auto-2 SSRF (MUST-ASK), Auto-11/17 (behavior-change real-bug), Auto-8/22 (TOCTOU).
- **Knowledge:** K-3 (Sector="eticaret" davranış+migration), K-8 (UpdateAsync tx/TOCTOU), K-11/15 (dedup), K-16 (scenario→faq).
- **WAA:** WAA-8 (ON CONFLICT latent), WAA-15 (DI dup). **Integrations:** Int-4 full pagination. **Backend:** kalan low/med. **Marketing-1.**
- Her batch = master'dan yeni `work/` branch → auto+Codex PASS → commit'te DUR → auth/Shared değişikliğinde MUST-ASK. Giriş: iki raporun §4 Quick Wins + §7.

## Platform & Gate

**Platform:** 11 .NET 8 mikroservis, tek shared PostgreSQL 16 + pgvector. Prod = services.invekto.com, `C:\Invekto\{Service}\current\`, NSSM. Portlar: Backend 5000, ChatAnalysis 7101, Appointments 7102, Knowledge 7104, AgentAI 7105, Integrations 7106, **Outbound 7107**, Automation 7108, WhatsAppAnalytics 7109, Marketing 7112, **VoiceAI 7114 (ayrı NSSM, server-deploy enum'da YOK → manuel zip)**. FaceAnalysis 7110 + VisualSearch 7111 = Planned. VoiceRuntime 7115 = ayrı NSSM.

**Fail-closed gate (2026-06-15):** Her servis TEK key gate'ler — ChatAnalysis=`Microservice:InternalApiKey`, WAA=`Benchmark:OpsKey`. Outbound↔Backend `InternalServices:SharedSecret` (iki uçta EŞİT). Config eksikse startup throw = DOWN → deploy öncesi CANLI doğrula.

**P0-3 Gate:** `BulkSend.AllowAllTenants=true` + `Projects=[5050, 15702882, 100000001]` + `CxapiSend=[5050, 100000001]` + `CxapiWebhookReconcile {Enabled:true, AllowAllConfigured:true}` + **`ProjectStatusPull {Enabled:true}`** (2026-06-17, vendor-scope CxapiSend allowlist'i reuse eder → otomatik delivered/read pull). Codex = gpt-5.5, LOW dahil her risk review.

**Pilot tenant gerçeği:** WapCRM-konfigli canlı = 5050 (vendor sandbox) + Medipol (100000001 wappflex, instance 8784 WABA). cxapi send + delivery-ack (PULL üzerinden CANLI) uçtan uca çalışıyor. Medipol = İKİ tenant satırı (mlpcm 15702882 + Medipol 100000001). **Medipol delivered/read webhook PUSH bağlı değil ama PULL (status-pull job) receipt'leri çekiyor (2026-06-17 doğrulandı).**

## Execution Queue (açık/pending — master: tracking/pilot-launch-roadmap.md)

- **REBRAND Chatinbox (Q-directed, off-roadmap, branch `rebrand/chatinbox`):** A1-A4 DONE (A1-A3 commit d33a4c42; **A4** hook regex [invariant-check/build-reminder/check-shared-microservice/dotnet-check.sh `Invekto\.`→`Chatinbox\.`] + PROJECT_CONFIG build-cmd [`Chatinbox.sln`/`Chatinbox.{SERVICE}.csproj`] = commit **b75a21cb**, build 0-err, Codex /rev PASS iter0; KORUNAN: INV- prefix, klasör yolu, Track-B prod host/NSSM; master'a merge YOK). SIRADA: **A5** frontend wordmark/title/copy + postman download literal (TenantsPage/DashboardPage.tsx) + Vite SPA rebuild (büyük faz → /clear öner); **A6** fresh-init ChatinboxAI.git + push (**Q ONAYI ŞART — dışa-dönük**, history alma). **Track B** (prod cutover) = chatinbox.net DNS + `*.chatinbox.net` cert + `destek@chatinbox.net` mailbox (**Q AKSİYONU, bu hafta paralel**) → sonra gated cutover (NSSM paralel-install, CxapiWebhookReconcile OwnedHosts ÖNCE, DB rename Faz B5 ertelenmiş, JWT Faz B6). MUST-NOT-TOUCH: /api/invekto/welcome, invekto- prefix, "InvektoServis" JWT literal. **B0 DNS dokümante** (B0 adım3): chatinbox.net A wildcard→`213.238.172.214` (BİZİM box; `91.151.84.3`=INMA app/apex; MX=M365); cert DNS-01 TXT; mail M365-mirror. **INMA-ertelenmiş paralel-domain:** chatinbox.net ayağa kalkar ama **PublicBaseUrl=services.invekto.com SABİT** → webhook invekto.com'da, reconcile partner-yazması YOK → **INMA dokunuşu SIFIR** (Q "bu hafta INMA'ya dokunamam"). AÇIK KARAR (Q): app./apex hedefi (bizim box vs landing+ertele) + wildcard-vs-açık-liste. Detay: `tracking/rebrand-chatinbox-migration.md`.
- **Projeler "26" (Q gözlemi 2026-06-17) — MUHTEMELEN ÇÖZÜLDÜ:** "26" = proje 7'nin (Medipol) **failed/ambiguous resendable mesaj sayısı** (error kodu DEĞİL); Q bunları "Yeniden gönder" ile yeniden göndermeye çalışıyordu → 503 (resend hotfix 130d4e85 ile fix). Eğer Q yine "26" görür + resend artık 200 dönerse kapat. Düşük öncelik.
- **Outbound `template_id is null` 500 = ✅ FİXLENDİ+DEPLOY 2026-06-17 (commit `72862ae4`, Codex iter0 PASS):** kök = `OutboundRepository.ReadCampaignResponse` nullable `outbound_campaigns.template_id`'i guard'sız `GetInt32(3)` okuyordu → `InvalidCastException` → `GetCampaignAsync`/`ListCampaignsAsync` HTTP 500. Fix: `IsDBNull(3) ? 0 : GetInt32(3)` (0 = "INSE template yok" sentinel, Shared contract değişmedi). Kardeş okuma site'leri (dequeue/BulkSend/Export) zaten guard'lıydı. Outbound deploy HEALTHY. NOT: NULL satırların provenance'ı kanıtlanmadı (CreateCampaign >0 zorlar → legacy/flow-insert olabilir); fix kaynaktan bağımsız doğru.
- **FEAT-IYS-INTEGRATION:** DRAFT-RESEARCH, build BLOCKED — İYS A.Ş. Faz 0 statü cevabı bekliyor. `tracking/feat-iys-integration.md`.
- **FEAT-INMA-PIPELINE-V2:** C4 + C3 hardening DONE+DEPLOYED. Kalan: canlı e2e smoke (5050 durum-değişikliği flow + action_set_customer_status node) = Q/INMA aksiyonu; Medipol (100000001) `webhook_secret` set değil (inbound-echo yarısı).
- **UP0.3** Tenant lifecycle handler: PENDING. **UP0.5** IInmaSendClient: PENDING.
- **PKT-13 Faz 1** Lead Scoring: PENDING. **FEAT-OBI Faz 2:** ✅ DONE+DEPLOYED 2026-06-17 (telefon tek-numara geçmiş arama, commit `5761ebbd`). **Faz 2.1** (geçmiş tablosu isimlendirme: Kampanya=proje adı, Şablon=WAPCRM adı, Mesaj=hover preview) = ✅ DONE+DEPLOYED 2026-06-18 (a7f2780a, aha-deploy'uyla birlikte prod'a çıktı). **Rapor Canlı İzleme AHA** (#1 auto-refresh/#2 progress/#4 failure-breakdown/#5 Esc) = ✅ DONE+Codex PASS iter3+DEPLOYED 2026-06-18 (`d642d8f4`+`868cc430`); UI fonksiyonel doğrulama Q'da.
- **FEAT-META-CAPI / META-ADS-INSIGHTS:** DRAFT (Q provision). META-MARKETING-API: BACKLOG ($50k+/ay gate).
- **Dent Adavista pilot:** BLOCKED (UP0 + FEAT-* sonrası). **Zoho OAuth smoke:** DEFERRED.

<!-- END_CURRENT_STATE max_lines=300 -->
