# Session Memory — Current State

> **BOUNDED FILE.** OVERWRITE edilir (append değil), ≤300 satır. Session init `limit=320` ile okur; sonundaki `END_CURRENT_STATE` işareti o pencerede görünmüyorsa DUR → `/optimize-memory`. Eski tam log: [session-memory-archive.md](session-memory-archive.md) (SADECE Grep). Kalıcı kararlar: [docs/decisions.md](docs/decisions.md). Standing kurallar: [hot-lessons.md](hot-lessons.md).
> **Pilot Mode:** queue+tracking otoritesi `tracking/pilot-launch-roadmap.md`'de; bu dosya son durum detayı.

_Güncel: 2026-06-13 (**FEAT-INMA-PIPELINE-V2 C3a `customer_status_changed` flow trigger DONE+DEPLOYED** — feat `d9a2ecc6` + spa-rebuild `665a50e7`, **Codex PASS iter0 [12/12 CQ + 4/4 CoVe]**). Backend `DispatchCustomerStatusFlowTriggers` (StoredApplied ONLY): fail-closed loop guard (`CustomerStatusFlowSuppression.Evaluate` — fire YALNIZ actor=user; api/zoho/system/null + `invekto-` originRequestId echo SUPPRESS) + change-gate (old≠new) + per-changed-lead Hangfire enqueue (G7, typed-catch **INV-INM-008**, HTTP 200 korunur). C2 repo `PersistAndApplyAsync` apply → tek **CTE FOR UPDATE+MATERIALIZED+ORDER BY id** per-lead old→new yakalar (monotonic guard KORUNDU); `AppliedLeads` eklendi. Automation `TriggerCustomerStatusFlowJob` [Queue automation, **AutomaticRetry=0**, synthetic chat -4M, **most-specific-wins** featureGroupId, **INV-AT-088**] + `CustomerStatusChangedTriggerHandler` (pure, 4 contract var) + tenant-scoped `GetActiveCustomerStatusFlowsAsync` + `FlowGraphV2.TriggerTypes`/`FlowValidator` +=. Shared `CustomerStatusFlowSuppression` + `OriginRequestIdPrefix='invekto-'` (C4 paylaşımlı) — 18/18 unit test. **Migration YOK.** **codex_consult(critique) implementasyon ÖNCESİ 3 gerçek bug yakaladı** → iter0: (1) CTE FOR UPDATE concurrency, (2) change-gate old≠new sahte-tetik, (3) fail-closed loop guard. **Full deploy Backend+Automation 10/10 HEALTHY**, config-restore'lu (webhook AllowedIps korundu), SPA `index-DB6P83VO.js`. **CANLI ama inert** (henüz customer_status_changed flow yok — C3b UI ertelendi). **Reliability hardening backlog** (go-live öncesi): bounded-retry+idempotency+outbox — AutomaticRetry=0 transient blip'te tek otomasyon kaybı (one-shot event, welcome'dan lossy). **Sıradaki: C3b** (Flow Builder UI node + cxapi feature-group katalog dropdown) → **C4** (Set Customer Status write-back).)_
_Önceki (aynı gün, cxapi): 2026-06-13 (**FEATURE C + FEATURE A BERABER DEPLOY ✅ — Q emri "a deploy etmeden c yi de bitir hepsini deploy et"**). **FEATURE C** cxapi stranded-'sending' periyodik recovery DONE+DEPLOYED — commit `f709989d`, **Codex PASS iter2** (12/12 CQ + 4/4 CoVe; iter0 FAIL = CQ3 plan-JSON declare + CQ9/Q1/Q2 false-positive [posting CAS diff dışıydı, koda kanıt yazıldı], iter1 FAIL = VQ-coverage Tenant/Auth eksik → Q4 eklendi). Migration **064** (additive `claimed_at` TIMESTAMPTZ + partial index `idx_outbound_messages_cxapi_sending`) prod'da çalıştı, kolon doğrulandı. `SweepStrandedSendingAsync` periyodik 'sending'→'queued' reset (claimed_at staleness-gated, StaleSendingMinutes=3, _isProcessing single-flight + posting CAS = duplicate-safe). **FEATURE A** (`6df65d7e`, önceki session) AYNI DEPLOY'la prod'a çıktı + **ENABLE: AllowAll** (Q kararı) — `CxapiWebhookReconcile {Enabled:true, AllowAllConfigured:true}` prod config (backup `bak-20260613-cxapi-webhook-reconcile`). Outbound publish (37 DLL, fresh 15:37) → stop→extract→start, **/health OK**, canlı log: `MessageSenderService starting` + `CxapiWebhookReconcileJob starting (allowAll=True, intervalMs=600000, publicBase=services.invekto.com)`, hata yok. Config-restore'lu (zip'ten prod placeholder çıkarıldı). Önce bu session: dashboard hızlı-erişim cleanup `1c1e2b73` (static-only, deployed). ⚠️ session-memory token-cap yüksek → `/optimize-memory` öner.)_
_Önceki: 2026-06-12 (Projeler rapor drawer TOPLU "Hataları Tekrar Gönder" butonu DONE+DEPLOYED — commit `fe28d4ae`, Codex PASS iter0 12/12 CQ + 4/4 CoVe; **FULL DEPLOY** 10/10 HEALTHY, SPA `ProjectsPage-WsjMSgkm.js`/`index-BPt6iCBj.js`, migration YOK; ⚠️ Marketing mid-deploy SSH channel-failure'dan MANUEL kurtarıldı [zip extract + previous/ config restore + nssm start]). ÖNCE: Medipol bulk takılma INCIDENT çözüldü + 3 paket DEPLOYED + Medipol webhook API ile set). DEPLOY ~11:47 TR: Outbound+Backend, 10/10 HEALTHY, config-restore'lu, migration YOK. SPA `index-BlvbQ1Zc.js` / `ProjectsPage-DPrkxY2J.js`. 3 commit: `e62ce0e8` worker-wedge fix + `0b7b638b` Rapor drawer + `d0557ba0` rapor 42P08 hotfix. cxapi self-serve webhook API keşfedildi (panel'e alternatif). Önceki akşam-8 SPA: `index-D9TAzY_W.js`. Outbound gerçek port 7107._
_2026-06-12 SON SESSION (DEPLOY + FEATURE B BATCH): (1) Projeler report/send UX pack + FEATURE B status-pull (`82e4f596`) Outbound+Backend+SPA DEPLOY edildi (~11:31, 10/10 HEALTHY, SPA `index-DvZmv06B.js`). (2) **message-status CANLI ŞEMA DOĞRULAMASI** — önceki "401 Invalid Request Model"ler PowerShell curl gövde quote-mangling artefaktıymış; endpoint baştan beri sağlam (`-d @file` ile Medipol WABA 8784 → status:true messageStatus=3 Viewed; 5050/6570 QR → 626 none-found). Doc batch'e güncellendi (`{instanceID, messageIDs[]}`, response `data.messages[]`). (3) **FEATURE B BATCH REWORK ✅ DONE+DEPLOYED (`63e9ef76`, Codex PASS iter0 12/12 CQ + 3/3 CoVe)** — `WapCrmSendClient.GetMessageStatusesAsync` (batch ≤100, `data.messages[]` typed parse) + caller resolved-instance grupla/chunk; singular `GetMessageStatusAsync` korundu (Q). Düzeltilen defect: singular parse top-level `data.messageStatus` arıyordu (canlı hiç dönmüyor) → "Durumu Yenile" inert'ti. Outbound+Backend deploy (~12:38, 10/10 HEALTHY). Plan `20260612-cxapi-message-status-batch.json`. (4) **FEATURE C plan yazıldı, PLANNING'de DURAKLATILDI** (Q kararı: önce batch). NOT: SPA `wwwroot/app` churn (Adım 1 rebuild) `/wrap` commit'iyle git'e alındı._

_İKİNCİ İNCİDENT (2026-06-12 ~13:18 TR, ops-only, KOD YOK): Medipol "test" broadcast (`729c1a20`, 9 alıcı) 3 mesaj 'Gönderiliyor'da takıldı. KÖK NEDEN: JWT crash (12:03 deploy anı, config 23-byte→33-byte düzeltildi, servis restart oldu) restart'ı tam gönderim ortasına denk getirdi → 3 mesaj 'sending'de strand (attempt_count=0, hiç POST edilmemiş). Periyodik recovery sweep yalnız 'posting' kurtarıyor, 'sending' reset startup-only → öksüz. ÇÖZÜM: 3 satır targeted requeue (`UPDATE 'sending'→'queued'`, duplicate-safe), worker ~3sn'de gönderdi → 8 sent / 1 ambiguous (+905424765425, teslim belirsiz, otomatik resend YAPILMADI — Q manuel kontrol edecek). Kalıcı kod fix Q kararıyla SONRAYA (FEATURE C, aşağıda)._

_FEATURE A ✅ DONE+DEPLOYED+ENABLED (2026-06-13): commit `6df65d7e` (Codex PASS iter1) cxapi message-webhook auto-reconcile sweep (Outbound `CxapiWebhookReconcileJob` + Shared `WapCrmWebhookSettingsClient`). **2026-06-13 FEATURE C ile beraber deploy + ENABLE: AllowAll** (Enabled=true + AllowAllConfigured=true, prod config). Canlı: `CxapiWebhookReconcileJob starting (allowAll=True, intervalMs=600000)`. İlk sweep startup+15s, sonra 10dk. Yabancı (non-Invekto) webhook URL'lere DOKUNMAZ; tüm WapCRM-konfigli tenant'ların ack webhook'unu `services.invekto.com/api/v1/webhook/event?companyId={tenant}` ile hizalar._

_PROJELER REPORT/SEND UX PACK + FEATURE B DONE (2026-06-12 — commit `82e4f596`, Codex PASS iter1 12/12 CQ + 3/3 CoVe; ⚠️ DEPLOY EDİLMEDİ): (1) Gönder modal'da onaylı şablon adı + yapılandırılmış full preview (header/body/footer/buttons). (2) Projeler tablosu içerik satırında portal-konumlu zengin hover önizleme kartı (overflow-hidden'ı aşar). (3) Tabloya Okundu (read_count) kolonu — sayaçlar AYRIK kova semantiği (Q kararı: move-forward, sent-- when delivered++). (4) Rapor drawer: Gönderim/İletildi/Okundu tarih-saat kolonları (+ sent_at recipients sorgu/DTO/api/CSV) + ilk açılışta SON gönderime default (runs[0]). (5) FEATURE B status-pull (yukarıda Execution Queue'da). Detay: aşağıda + plan `20260612-projeler-report-ux-statuspull.json`._

## Current State Snapshot
- **2026-06-13 FEAT-INMA-PIPELINE-V2 C2 ✅ DONE+DEPLOYED (`ab2e644e`, Codex PASS iter2: 12/12 CQ + 5/5 CoVe).** INMA `customer.selection_changed` signed-event consumer, EXISTING `/api/v1/webhook/event?companyId=X` (yeni `type` dalı msg/ack ÖNCE). Fail-closed HMAC-SHA256 `{X-Invekto-Timestamp}.{rawBody}` (secret=`settings_json->'inma'->>'webhook_secret'`) → dedupe(tenant_id,event_id)+raw audit (1 tx) → opaque `leads.customer_status`. **Migration 065 prod'da çalıştı** (customer_status + occurred_at + functional phone idx + `inma_webhook_events`), Backend deploy HEALTHY. MVP: flow-trigger forward=C3, phone-miss=drop-with-audit+WARN. INV-INM-001..007. **Follow-up: Q `webhook_secret`'ı 5050 settings_json'a koyacak; ilk canlı event'te Backend JSONL `sigEnc=` log'undan encoding doğrula → config lock + freshness aç.** **✅ VERIFY-LIVE PASS (2026-06-13, commit 3667f0ca):** gerçek 5050 event'i uçtan uca doğrulandı (sigEnc=hex, leads.customer_status='Uçak Bileti', occurredAt guard OK). İmza reçetesi: key=utf8(base64-secret-STRING, decode YOK) + msg=`{unixSecondsTs}.{rawBody}` + digest=hex `sha256=` prefix. **2 BLOKER bulundu+çözüldü:** (1) events webhook AYRI IP'den geliyor → `78.135.105.25` prod Webhook:AllowedIps'e eklendi (events kanalı IP-gate'inde 401 yiyordu, HMAC'e ulaşamıyordu). (2) HMAC reçetesi reverse-engineer (geçici diag). EnforceTimestampFreshness hâlâ OFF (dedupe replay'i zaten kapatıyor; format=unixSeconds doğrulandı, istenirse tek flag).
- **⚠️ İZLE:** Backend stderr'de tarihsel `Body was inferred but the method does not allow inferred body parameters` crash trace (C2 DEĞİL — C2 route eklemiyor; canlı PID 14860 sağlıklı). Ayrı incelenmeli.

**Platform:** 11 .NET 8 mikroservis, tek shared PostgreSQL 16 + pgvector. Prod = services.invekto.com, `C:\Invekto\{Service}\current\`, NSSM. Portlar: Backend 5000, ChatAnalysis 7101, Appointments 7102, Knowledge 7104, AgentAI 7105, Integrations 7106, **Outbound 7107**, Automation 7108, WhatsAppAnalytics 7109, Marketing 7112. FaceAnalysis 7110 + VisualSearch 7111 = Planned. VoiceRuntime + VoiceAI = ayrı NSSM (server-deploy enum'da EKSİK — L-2026-05-31).

**Aktif iş: FEAT-PROJELER (PKT-14) — cxapi/WapCRM bulk-send motoru. CANLI + Medipol delivery-ack loop API ile açıldı.**
- **2026-06-12 RAPOR DRAWER TOPLU RESEND ✅ DONE+DEPLOYED (`fe28d4ae`, Codex PASS iter0 12/12 CQ + 4/4 CoVe).** Q: "rapor drawer üst tarafa hata alanları tekrar gönder düğmesi ekle". Tekil per-satır resend yanına projedeki TÜM `failed`/`ambiguous` alıcıyı tek SQL transaction'da yeniden kuyruğa alan toplu akış: Shared `ProjectResendBulkResultDto {requeued}` + Outbound `RequeueAllForResendAsync` (CTE: tgt FOR UPDATE → reset → per-broadcast agg → sayaç decrement + completed→sending re-open; COUNT döner) + `ResendAllAsync` (404 ownership probe; requeued>0 ise RecomputeRollup; 0=başarılı no-op) + `POST /report/resend-bulk` endpoint + Backend proxy + api.ts + ProjectsPage üst toolbar butonu (kapsam=TÜM proje, eligible=Σ failed+ambiguous, 0'da disable) + z-60 onay modalı + in-flight guard. Q kararları: kapsam=tüm proje hataları, uygulama=backend bulk endpoint. Eligibility/ownership tekil resend ile birebir. **FULL DEPLOY** (Shared.dll değişti → 10 servis): Backend+Outbound feature canlı, kalan 8 binary-consistency; Marketing mid-deploy SSH-failure → manuel recovery (L-2026-06-12). Plan JSON yok (ad-hoc).
- **2026-06-12 INCIDENT: MEDIPOL BULK TAKILDI → WORKER-WEDGE TEŞHİS + KURTARMA.** Q "medipol bulk gönderdim, hâlâ gönderiyor gibi". KÖK NEDEN: Outbound `MessageSenderService.ProcessQueue` tek-thread + `_isProcessing` interlock; bir cxapi POST asıldı (HttpClient.Timeout=Infinite, tek koruma 10sn linked-CTS propagate olmadı) → worker süresiz wedge, servis HEALTHY ama 4 saat hiç gönderim yok (8 mesaj: 4 sent, 1 posting, 3 sending). KURTARMA: `nssm restart InvektoOutbound` → startup recovery (3 sending→queued resend, 1 posting→ambiguous) → 7 sent/1 ambiguous. Proje rollup elle recompute → completed (idempotent app SQL). 5050 proje 1 de aynı şekilde completed.
- **2026-06-12 WORKER-WEDGE FIX ✅ DONE+DEPLOYED (`e62ce0e8`, Codex PASS iter1 12/12+3/3).** WapCrmSendClient+TemplateClient `HttpClient.Timeout`: Infinite → FINITE backstop (TimeoutMs+buffer, ≥1s floor) + periyodik staleness-gated 'posting' recovery sweep (CxapiSendOptions.RecoverySweepIntervalMs=2dk, _isProcessing guard içinde, restart'sız self-heal). 'sending' reset startup-only kalır. WapCrmSendOptions/WapCrmTemplateOptions += HttpClientBackstopMs (additive). iter0 FAIL (bare catch + INV kod + db-sync) → iter1 fix. Plan `arch/plans/20260612-outbound-sender-wedge-fix.json`.
- **2026-06-12 RAPOR DRAWER ✅ DONE+DEPLOYED (`0b7b638b`, Codex PASS iter1 12/12+3/3).** Projeler satırında **Rapor** butonu → geniş sağ drawer: gönderim (run) dropdown + server-paged kişi/durum tablosu (search + campaign filtre) + gitmeyene (failed/ambiguous) **Tekrar Gönder** + filtrelenmiş **CSV export**. Backend: 3 Outbound endpoint (`projects/{id}/report/runs|recipients|resend`) + 3 proxy; GetRecipientsAsync paged ILIKE; RequeueForResendAsync atomik re-queue (failed/ambiguous→queued, eski kova--/queued++, broadcast reopen) → MEVCUT worker resend eder (yeni send-path yok). INV-OB-090 (resend not-eligible). Resend SADECE failed/ambiguous (Q); CSV TÜM filtrelenmiş; özet sayaç partition kalır (Q). Q karar `partition kalsın`. Plan `arch/plans/20260612-projeler-rapor-drawer.json`.
- **2026-06-12 RAPOR 42P08 HOTFIX ✅ DONE+DEPLOYED (`d0557ba0`, Codex PASS iter0).** Rapor recipients 503/INV-OB-071 veriyordu → `42P08 could not determine data type of parameter $3` (nullable @campaign/@search DBNull untyped, ilk `@x IS NULL` bağlamında). FIX: paylaşılan `filter` SQL'de `::text` cast. authed probe 200 doğrulandı.
- **2026-06-12 5050 CANLI DELIVERY/READ TESTİ PASS:** msg 30 `sent→delivered(+4sn)→read` gerçek INMA ack'leriyle uçtan uca doğrulandı. Bizim ack zinciri (PR-3b) + companyId=tenant_id kontratı canlı kanıtlı.
- **2026-06-12 MEDIPOL ACK WEBHOOK API İLE SET ✅ (config-only, panel bypass).** cxapi self-serve `POST /api/webhook-settings/messages` (X-CIB-SecretKey=Medipol secret, sunucudan IP-whitelistli) → `webhookUrl=...webhook/event?companyId=100000001`, isActive=true. read-back verified. Q panelden yaptığı kayıt `CompanyWebhookEndpoints`'e düşmemişti; API doğrudan yazdı. **companyId = BİZİM tenant_id (100000001), INMA CompanyID 3944 DEĞİL** (5050'de de companyId=5050=tenant, INMA CompanyID 11 değil). Yeni Medipol gönderimleri delivered/read izlenecek (eski 7 'sent'te kalır). Doküman: `temp/invekto-api-webhook-settings-and-message-status-for-agents.md`.
- **2026-06-11 AKŞAM-7: LİSTE VIEWER TEKİL KAYIT EKLE/SİL ✅ DONE+DEPLOYED (`cdc22980`; akşam-8 toplu deploy'la canlı — Outbound+Backend+SPA `index-D9TAzY_W.js`, endpoint'ler JWT'siz 401 smoke OK) (Codex 3-chunk: c1 Outbound + c2 Shared/proxy PASS iter0; c3 UI iter0 FAIL [GERÇEK bulgu: 089 recovery'de bare catch{} sessiz yutma] → fix → iter1 PASS. Plan-consult 7 bulgu: 4 kabul [2 blocker: FOR UPDATE lock-first tx + ConstraintName filtresi], 3 corrected-premise red. Pre-rev 5-lens adversarial verify [Workflow]: paralel-session ConnectionType hunk SIZINTISINI yakaladı [diff kendi başına derlenmezdi] + 2 gerçek UI bulgusu commit ÖNCESİ fixlendi. UI yarısı paralel sweep `c8a20e04`/`e516a1df` ile master'a önceden girmişti — revert gerekirse iki commit seti birlikte düşünülmeli.)** Q isteği: viewer popup'a telefon çıkartma + yeni ekleme. KAPSAM: POST /data-lists/{id}/records (201) + DELETE .../records/{recordId} + 2 proxy; repo data_lists FOR UPDATE lock-first tek tx (status/cap kararı + INSERT/DELETE + COUNT recompute serialize — READ COMMITTED blocked-UPDATE subquery anomalisi kapandı); duplicate = uq_list_record_list_phone constraint-filtered 23505 → **INV-OB-088**/409 (SELECT pre-check YOK); record-not-found → **INV-OB-089**/404; manuel eklemede geçersiz telefon REJECT (047 — import'un sendable=false davranışından bilinçli ayrışma, Q kararı); status!='ready' 051/409 kilit altında. ListRecordDto += id (additive, MapRecord 13 kolon, preview-sample dahil). UI: Yeni Kayıt paneli (telefon* + ad/soyad/e-posta, TR hata haritası, başarıda son sayfaya atlama [ORDER BY id → yeni kayıt son sayfada, gap-hunt bulgusu]) + satırda 2 aşamalı çöp ikonu (3sn disarm; İptal butonu yok — Q modal kuralı) + sayaçlar response'tan senkron + row key=id. Plan `arch/plans/20260611-list-record-add-remove.json`.
- **2026-06-11 AKŞAM-6: TR DİAKRİTİK SWEEP ✅ DONE+COMMITTED `c8a20e04` (Q FORCE PASS — iter1'de sweep'in kendi seti Codex'te temiz [Q1+Q3 PASS], kalan blocker'lar yalnız paralel-session sızıntısı; override plan.overrides[]'da). ✅ DEPLOYED (akşam-8 toplu deploy, SPA `index-D9TAzY_W.js`).** Q isteği: "Hoşgeldiniz + başka tr sorunları"; kapsam kararı: TÜM SPA UI metinleri (flow-templates.ts şablon içeriği HARİÇ). YÖNTEM: 102 dosyaya paralel agent Workflow (sonnet) — kurallar: yalnız render edilen metin, identifier/key/route/karşılaştırma/export-header ASLA, kelime seçimi değişmez, 'Hoşgeldiniz' bitişik (glossary). SONUÇ: 86 dosya/~1790 satır (OnboardingGuidePage 512, node-metadata 89, NodePropertyPanel 78...). **DETERMİNİSTİK KANIT:** `c:/tmp/verify-diacritics-diff.ps1` — 1747 satır-çifti iki-taraf-diakritik-soyma sonrası birebir = saf-diakritik İSPATI; ~50 flagged = orijinal yazım hatası düzeltmeleri (Codex Q3 PASS); 1 ajan hatası (cevaplayıyor→cevaplıyor) elle fix. **RACE:** verifier↔commit arası paralel session'ın Layout/DataImportPage/ProjectsPage yapısal WIP'i sızdı → telafi `e516a1df` (api.ts tip-tutarlılık) + sızan parçalar sahibi commit'lerde resmileşti (60c32cd3, f9bc3c24, 7966d4b5). Plan `arch/plans/20260611-tr-diakritik-sweep.json`.
- **2026-06-11 AKŞAM-5a: MEDIPOL CXAPI PROVISIONING ✅ CANLI (config+data-only, kod yok — cxapi runbook adım 1+2).** Q raporu: "medipolde waba numarası gelmiyor ama 1 tane var" → KÖK NEDEN ZİNCİRİ: tenant 100000001'de `settings_json->wapcrm` YOK → Backend instance fetch edemiyor → `tenant_instances` boş → dropdown boş. INMA tarafı: `medipol` (CompanyID 3944) ApiKey NULL + ApiStatus NULL idi. **ÇÖZÜM:** (1) Q INMA panelden key üretti (`Companies.ApiKey` = bizim secret_key — 5050 ile birebir doğrulandı) + ApiStatus aktifledi (false iken cxapi 501 ACCESS DENIED, canlı test 2x); (2) tenant 100000001 `settings_json->wapcrm` backfill: secret + user_id=1 ('Admin', WaClientmedipol.Users; 5050'deki user 12 'taner' deseni) + instance_id=8784 + cxapi api_url (read-back verified; önceki değer NULL); (3) **CxapiSend allowlist `[5050]` → `[5050, 100000001]`** (.bak `bak-20260611-cxapisend-medipol`, targeted replace + JSON validate + InvektoOutbound restart + health ok) — Q emri "izin ver" (INV-OB-085 ekran görüntüsü). Medipol cxapi'de 2 instance: **8784 'Medipol API' = WABA (905017287587)** + 7041 'medipol' = QR Code; cache 14:15'te doldu. **KALAN (ack-loop):** medipol'ün `CompanyWebhookEndpoints` kaydı YOK (0 satır) → delivery ack gelmez, mesaj 'sent'te kalır; INMA'ya webhook URL ekletilmeli.
- **2026-06-11 AKŞAM-5b: PROJELER DROPDOWN WABA-ONLY ✅ DONE+COMMITTED `f9bc3c24` (Codex PASS iter2 — 12/12 CQ + 5/5 CoVe; iter0 FAIL = paralel session'ın uncommitted Program.cs hunk'ı pathspec diff'e karıştı [kod hatası değil, diff hijyeni], iter1 FAIL = 'varchar=int binding' FALSE-POSITIVE [pre-existing satır 698 ToString TEXT binding; dar U3 context göstermiyordu → -U14 ile iter2 PASS; kod iter0'dan beri aynı]). ✅ DEPLOYED (akşam-8: migration 063 + backfill [5 WABA / 3 QR / 8 NULL verified] → Outbound → Backend+SPA).** Q: "dropdown'da yalnız waba hatlarını göster". KANIT: cxapi instanceType=1'i HEM WABA HEM QR'a veriyor (5050: 2131+8523 type=1 'QR Code'; 4 gerçek WABA: 4708/5623/6570/7495) — ayırt edici `connectionType`. PAKET: migration **063** tenant_instances += `connection_type TEXT NULL` (INV-SEED-063) + Shared DTO nullable ConnectionType + Backend fetch map (trim/boş→null) + upsert `COALESCE(EXCLUDED, mevcut)` (degraded vendor cevabı iyi değeri ezmez) + **Outbound `IsCloudApiInstanceOwnedAsync` STRICT `UPPER(TRIM(connection_type))='WABA'` — NULL GEÇMEZ** (plan-consult bulgusu; Q onayı: interview'daki NULL-toleransı kararı revize edildi) + SPA filtresi (null→eski instanceType===1 fallback, display-only — bilinçli asimetri). SPA parçaları (ProjectsPage filtre + api.ts InstanceDto.connectionType) paralel commitlerle (`c8a20e04` süpürme + `e516a1df` tip fix) ZATEN master'da; `f9bc3c24` kalan backend/DB/Shared parçaları. **DEPLOY SIRASI KRİTİK: migration 063 → DATA BACKFILL (5050'nin 14 + medipol'ün 2 satırı canlı cxapi değerlerinden; tenant_instances'ta BAŞKA tenant YOK — DB kanıtlı) → Outbound → Backend+SPA.** Backfill'siz Outbound deploy'u 5050/medipol test-send'ini kırar (strict NULL reddi). QR'a bağlı mevcut proje yok (tek proje 6570=WABA). Plan `arch/plans/20260611-projeler-waba-only-dropdown.json`.
- **2026-06-11 AKŞAM-4: TENANT NAV — ŞABLON GALERİSİ TENANT'TAN KALDIRILDI ✅ DONE+COMMITTED (`60c32cd3` + diakritik-restore `7966d4b5`, Codex PASS iter0 — 12/12 CQ + 3/3 CoVe; **NO DEPLOY** [Q kararı: tree'de review'suz WIP var, sonraki deploy'la çıkar]).** Q: 'tenant sol navdan şablon galerisini kaldır'. İki nav kaynağı senkron: (1) Layout.tsx '/flow-templates' tenant 'Çalışma Alanı' bloğundan ops 'İçerik' bloğuna opsOnly:true (tenantLabel+FlowBuilder gate kalktı; tenantId=0 ops session + no-session ops görür, galeri 'Kullan' butonu no-session'da guard'lı no-op); (2) /api/v1/inma/nav workspace'ten flow-templates InmaNavItem silindi (dual-source senkron korundu). Route+sayfa bilinçli korundu (direkt URL + FlowListPage linkleri çalışır). **PARALEL-SESSION YARIŞI:** cerrahi commit eski-HEAD-bazlı Layout blob taşırken TR-sweep `c8a20e04` araya girdi → `60c32cd3` sweep'in 11 Layout diakritik satırını istemeden revert etti (sweep de benim working-tree nav edit'imi süpürmüştü); reset `e516a1df` üste binince imkânsız → fix-forward `7966d4b5` (Layout = sweep+nav birleşik doğru blob). Plan `arch/plans/20260611-tenant-nav-sablon-galerisi.json`.
- **2026-06-11 AKŞAM-3: MEDIPOL PROJELER GATE-OPEN ✅ CANLI (config-only, kod değişikliği YOK — Codex kapsamı dışı, 5050/cxapi gate-open pattern'i).** Q emri: "medipol firmasına projeler özelliğini aktif yap". Outbound prod `Projects.AllowedTenantIds`: `[5050]` → `[5050, 15702882, 100000001]` (.bak: `bak-20260611-projects-medipol`; targeted text-replace + ConvertFrom-Json validate + `InvektoOutbound` restart [ProjectsOptions startup-bind, restart şart] + health 10/10). **İKİ Medipol tenant satırı bilinçli birlikte eklendi:** eski `mlpcm` 15702882 (2026-04-02, wapcrm portal girişleri) + yeni `Medipol` 100000001 (2026-06-11 16:17 wappflex lazy-provision; adı `4fe70e65` data-fix'iyle 'Admin'→'Medipol' düzeltilmişti) — wappflex token'ı `medipol` company code taşıyor (mlpcm'e ÇÖZÜLMÜYOR) → iki satır = iki ayrı veri alanı; Medipol hangi portaldan girerse girsin Projeler açık. **Kapsam:** Projeler CRUD+UI+test-send aktif (BulkSend zaten tüm tenantlara açık → bridge route, yalnız inbound-geçmişli numara); HSM/cxapi DEĞİL (CxapiSend hâlâ [5050] + iki tenant'ta da `settings_json->wapcrm` YOK — gerçek kampanya için cxapi runbook 3-adım gerekir).
- **2026-06-11 AKŞAM-2: DASHBOARD KİMLİK + TENANT PROVISION ADI FIX ✅ DONE+DEPLOYED (`4fe70e65`, Codex iter0 FAIL [CQ2 sessiz catch + CQ5 null-forgiving — gerçek bulgular] → iter1 PASS 12/12+4/4; deploy ~16:52 TR, SPA `index-4QK6z2_3.js` [wappflex+login-UI paketlerini de içerir], 10/10 HEALTHY, config-restore'lu).** Q raporu: dashboard 'Demo User' gösteriyordu + Firmalar'da Medipol 'Admin' adıyla açılmıştı. **KÖK NEDENLER:** (a) INSE JWT isim claim'i taşımıyor → getSession INSE dalı hardcoded 'Demo User' (login/exchange yanıtındaki full_name çöpe gidiyordu); (b) `ResolveInmaTenantAsync` provisioning'e kullanıcının FullName'ini firma adı olarak geçiyordu → Medipol SSO ilk girişi tenant_name='Admin' yarattı. **FIX:** (1) api.ts full_name kimliğe bağlı ({name,tid,uid}) localStorage persist — token'dan ÖNCE yazılır, yalnız source inma_exchange/inma_mock + kimlik eşleşirse okunur, ops etiketleri bit-identik, removeTokens temizler, bozuk kayıt log+cleanup; (2) TenantDashboardPage 'Firma #' satırı kaldırıldı; (3) provision adı: login=yazılan firma adı (trim+200cap), exchange/bearer=CompanyCode — kullanıcı FullName'i ASLA. **(4) PROD DATA FIX (Q onaylı, read-back verified):** 100000001 'Admin'→'Medipol' + 100000000 'zübeyde ebrumoda'→'Ebrumoda' (plan-consult PR-008 taraması ikinci bozuk satırı buldu). Plan `arch/plans/20260611-dashboard-user-tenant-name.json`.
- **2026-06-11 AKŞAM: MEDIPOL WAPPFLEX SSO FIX ✅ DONE+DEPLOYED (`bfafdfdf`, Codex PASS iter0 — 12/12 CQ + 3/3 CoVe; deploy ~16:16 TR, SPA `index-B0mSILRy.js` [login-UI paketini de içerir, 16:09'daki `index-B9Kn8nc-.js` deploy'unu süperseed etti], health OK, config-restore'lu).** Medipol (MLPCM) WapCRM **white-label portalı `app.wappflex.com`**'dan INSE embed'i login ekranına düşüyordu. KÖK NEDEN (5-agent workflow, prod-doğrulamalı): inmaBridge `INMA_ALLOWED_ORIGINS`'te wappflex yok → `inma:auth` postMessage SESSİZCE düştü (log imzası: `inma/nav 200` → ~1sn sonra `onboarding/status 401 tenantId='-'`); ikincil: `InmaNavCors` CORS'ta da yok (nav fallback'le maskeliydi). **FIX (additive 3-nokta):** bridge origin listesi + InmaNavCors + `INMA_API_BASE_URL_REGEX` `(wapcrm.net|wappflex.com)` (white-label parent kendi apiBaseUrl'ini gönderirse INV-INT-105 TÜM auth mesajını setAuth öncesi reddederdi — origin tek başına yetmezdi; inmaApiClient'in şu an 0 consumer'ı var, regex genişlemesi runtime'da inert). INMA notice doc senkron (bridge+CORS listeleri özdeş kuralı, 20260416 Q1). Smoke: prod bundle `findstr wappflex` ✓ + preflight 204 + `Access-Control-Allow-Origin: https://app.wappflex.com` ✓. Server-side DEĞİŞMEDİ: mlpcm (15702882) tenant kayıtlı → wappflex token'ları aynı INMA backend'de (testapi.wapcrm.net welcome) doğrulanır; Medipol ayrı satır gerekmez (`ResolveOrCreateByInmaCodeAsync` lazy-provision). Plan `arch/plans/20260611-wappflex-origin-whitelist.json`. **Paralel-session notu:** aynı tree'de login-UI paketi eşzamanlı wrap'lendi — bu paketin commit'i pathspec-scoped yapıldı, çakışma yok; asset rotasyonu (B0mSILRy) bu wrap commit'inde.
- **2026-06-11: LOGIN UI TEMİZLİĞİ ✅ DONE+DEPLOYED (`d4a2c45d`, Codex PASS iter0 — 12/12 CQ + 3/3 CoVe; deploy ~16:09 TR, SPA `index-B9Kn8nc-.js`, 10/10 HEALTHY, config-restore'lu — webhook IP korundu).** Q isteği (2 GR, UI-only): **(1)** 'Test Girisleri' mock-login bölümü (Tam Yetkili/Demo Klinik/Demo Otel) login ekranından TAMAMEN kaldırıldı — zincirin kalanı BİLİNÇLİ duruyor (useAuth.loginWithMock + api.mockLogin + backend `/api/v1/inma/auth/mock-login`, `InmaAuth:MockEnabled` config-gated, kapalıyken 503); **(2)** logo yatay kırmızı marka logosu (ikon + #E54C4C 'invekto', suffix yok). Plan-review (codex_consult, verdict: modify) gerçek bulgu: variant='none' zorlamak super.invekto.com ops girişinin 'super' suffix'ini de silerdi → Q kararı: **super mevcut dikey navy logoyu korur** (hostConfig.superHost ternary). InvektoLogo: opsiyonel `color` prop (default navy, sadece ana wordmark) + suffix'siz modda SVG genişliği wordmark'a daralır (sağ ölü boşluk fix). Plan `arch/plans/20260611-login-test-girisleri-logo.json`.
- **2026-06-10 AKŞAM: PROJELER LİSTE UX ✅ DONE+DEPLOYED (`4115fcd2`, Codex iter0 FAIL→iter1 PASS 12/12+3/3; deploy ~18:19 TR, SPA `index-BKdCQuBe.js`, 10/10 HEALTHY).** Q 3-madde: Yenile butonu (loadProjects, loading spin) · durum chip'leri → select dropdown (statusFilter bit-identik) · satırda atanan içerik adı (şablon adı/galeri adı/serbest metin) + native title tooltip'te tam içerik. Şablon adları distinct-instance başına tek `getWaTemplates` fetch (mevcut JWT proxy); iter0 Codex bulgusu fix: hata → console.warn + 'error' state ('yüklenemedi — Yenile ile tekrar deneyin' tooltip) + Yenile retry (ref delete). Plan `arch/plans/20260610-projeler-list-uiux.json`.
- **2026-06-10 ÖĞLE-4: HSM HEADER-PARAM VENDOR GAP ✅ TESPİT+UI UYARISI DONE+DEPLOYED (`bc4b705a`, Codex PASS iter0 — 12/12 CQ + 2/2 CoVe; deploy ~14:41, SPA `index-DkW2SyMn.js`, 10/10 HEALTHY).** Q raporu: HSM test header'ı önizlemeden farklı geldi ("Test taner" → "Test Müşterimiz"). **KÖK NEDEN VENDOR-SIDE (canlı kanıtlı, bizim wire DOĞRU):** cxapi `POST /api/templates` HEADER text değişkenlerini requiredInputs'ta DÖNMÜYOR (instance 6570'te 9/9 şablon sistematik; BODY `name` + BUTTON `btn1_url` dönüyor) VE `chatoperation` gönderdiğimiz `hname` paramKey'ini UYGULAMIYOR — header'a şablonun Meta-onaylı örnek değeri basılıyor. Kanıt: DB `outbound_messages.template_params={name:"taner",hname:"taner"}` (ikisi de wire'a çıktı, body uygulandı) + prod-sunucudan canlı template-list çekimi (secret server-side, psql+Invoke-RestMethod). **Mitigasyon (UI-only, +22 satır ProjectsPage.tsx):** `vendorUnappliedKeys` (preview placeholder ∉ requiredInputs[kind=text].paramKey) → param eşleme bölümünde amber uyarı ("alıcıya onaylı varsayılan değer gider; eşleme kaydedilir, WapCRM destek ekleyince otomatik geçerli"). Eşleme+wire BİLİNÇLİ aynen — forward-compat: INMA fix'leyince gönderdiğimiz değer otomatik uygulanır, uyarı requiredInputs'a anahtar gelince kendiliğinden kaybolur. **INMA BUG RAPORU Q'ya kopyala-gönder teslim edildi** (talep: (1) requiredInputs'a `{kind:text, location:HEADER, paramKey}`, (2) chatoperation header param eşlemesi). Plan `arch/plans/20260610-hsm-header-param-vendor-gap.json`.
- **2026-06-10 ÖĞLE-3: HSM TEST = GERÇEK TEMPLATE WIRE MODELİ ✅ DONE+DEPLOYED (`e68b798a`, Codex PASS iter0 — 12/12 CQ + 5/5 CoVe; deploy ~14:07, SPA `index-BjTUAJmH.js`).** Q düzeltmesi: "template gönderim modeli yanlış" — dün geceki "HSM testi render-metin düz gider" yaklaşıklığı SUPERSEDED (gerekçesi PR-4 yokluğuydu; artık canlı). **(1)** `ProjectTestSendRequest` += `instance_id/wa_template_id/template_params/template_header_media_url` (additive; EXACTLY-ONE: text XOR template). **(2)** `TestSendAsync` HSM dalı: instance ownership (`IsCloudApiInstanceOwnedAsync`, INV-OB-086) + param caps (≤32, ≤1024) + http(s) media URL → `BroadcastSendRequest{WaTemplateId, Recipients[0].Variables, TemplateHeaderMediaUrl}` + `overrideInstanceId` → MEVCUT PR-4 zinciri (orkestratör HSM üretimi → `SendTemplateAsync` pinli `TemplatePayload` — guide §3.2 birebir: `template{templateId, parameters[{paramKey,value}], headerMedia}`, language YOK). Test yolunda canlı katalog doğrulaması BİLİNÇLİ yok (run-path koruması); cxapi 621/622 reddi UI'da görünür. **(3)** `ToCxapiPhone`: cxapi `chatPhoneNumber` artık `+`'sız (guide §2 kontratı; yalnız cxapi callsite'ları — bridge kendi regex'i). **(4)** `failed_reason_sample = COALESCE(failed_reason, provider_error_message)`. **(5)** UI: HSM testte param değerleri örnek alıcıdan; örnek yoksa paramKey görünür dummy; not metni "GERÇEK onaylı şablon formatında". Plan `arch/plans/20260610-hsm-test-wire-model.json`.
- **2026-06-10 ÖĞLE-2 EK: İLK CANLI CXAPI GÖNDERİMİ BAŞARILI (10:47, broadcast `00ea869c`):** route=`wapcrm_cxapi`, instance 6570, `sent=1`, provider `Success`, **wamid `external_message_id`'ye yakalandı** (`wamid.HBgMOTA1…`) — PR-2/3a/3b zinciri uçtan uca canlı doğrulandı. Wire: `{instanceID:6570, userID:12, chatPhoneNumber, messageType:1, messageText}` + `X-CIB-SecretKey`.
- **2026-06-10 ÖĞLE-2: CXAPI GATE-OPEN (5050, bilinçli test-validasyonu) ✅ CANLI (Q emri: "testi cxapiden atman lazım, full test, farklı yol kullanma" — gate-memory'deki seçenek (ii)).** Config+data-only (kod değişikliği YOK): **(1)** tenant 5050 `settings_json->'wapcrm'.instance_id = 6570` backfill (INMA MSSQL readonly doğrulandı: 6570 = "WapCRM Official", InstanceType=1 Cloud API, aktif, Company Code=5050 "Regex Danışmanlık15" sandbox, tel 14845691146); **(2)** Outbound prod config `CxapiSend: {Enabled:true, AllowedTenantIds:[5050]}` (.bak: `bak-20260610-cxapisend-5050`) + restart, health OK. **YENİ ROTA GERÇEĞİ: 5050'nin TÜM broadcast'leri (test-send dahil) artık `wapcrm_cxapi` rotasında** — WapCrmSendClient → cxapi.wapcrm.net/api/chatoperation, wamid → external_message_id (PR-3b zinciri ilk kez canlı egzersiz). HSM (PR-4) yolu da 5050 için artık allowlist'ten geçer (canlı template-list doğrulamasıyla). **ACK SINIRI: sandbox'ın CompanyWebhookEndpoint'i `webhook.site/b2ec...`'e işaret ediyor (Enabled=true ama BİZİM endpoint DEĞİL) → delivery ack'leri bize GELMEZ, mesaj 'sent'te kalır; full ack-loop için INMA'dan webhook URL'ini `https://services.invekto.com/api/v1/webhook/event?companyId=5050`'ye çevirmesi istenmeli.** Diğer tenant'lar CxapiSend'de değil → onlar bridge'de kalır.
- **2026-06-10 ÖĞLE: BRIDGE-URL FIX + TEST-SEND SONUÇ GÖRÜNÜRLÜĞÜ ✅ DONE+DEPLOYED (`6eccf8ff`, Codex PASS iter0 — 12/12 CQ + 4/4 CoVe).** Q'nun ilk canlı test-send'i (09:24, broadcast 3459269e) bridge'de 4x HTTP 405 ile düşmüştü — ROOT CAUSE: prod `Callback:DefaultCallbackUrl=http://localhost:5000` PATH'SİZ (Backend root'a POST); doğru hedef Backend'in WapCRM köprüsü `/api/v1/callback/wapcrm` (Program.cs:8581 — tenant wapcrm settings + telefonun son INBOUND mesajının instance_id'siyle cxapi chatoperation'a iletir; Q'nun numarası↔5050 inbound geçmişi VAR, instance 6570 → teslim mümkün). **Fix 2 parça: (1) config-only** `DefaultCallbackUrl` += path (.bak: `bak-20260610-bridgeurl`); **(2) kod:** `BroadcastStatusResponse.failed_reason_sample` (additive nullable, tenant-scoped subquery ilk failed mesajın nedeni) + `api.getBroadcastStatus` (mevcut JWT'li proxy) + ProjectsPage `sendTest` 202'de durmayıp 10×1.5s NİHAİ sonucu yoklar: gönderildi ✓ yeşil / GÖNDERİLEMEDİ+neden kırmızı / completed-ama-sıfır engelleme uyarısı / netleşmedi nötr (sahte-yeşil imkânsız; buton poll boyunca disabled). Deploy ~13:36: Outbound+Backend, 10/10 HEALTHY, SPA `index-CO_yUrwY.js`; gate'ler değişmedi (BulkSend.AllowAllTenants=true, CxapiSend YOK). **ÖNEMLİ NOT: bridge rotası tasarım gereği yalnız INBOUND geçmişi olan numaralara gönderebilir (GetLastInstanceIdAsync) — inbound'suz numaraya bridge test'i NO_INSTANCE_ID ile düşer (artık UI'da görünür); soğuk/cold gönderim cxapi rotasının işi (CxapiSend gate'i hâlâ Q'da).** Plan `arch/plans/20260610-bridge-url-test-feedback.json`.
- **2026-06-10 SABAH: BULKSEND GATE-OPEN ✅ DONE+DEPLOYED (`793f0dad`, Codex PASS iter2 — 12/12 CQ + 5/5 CoVe, FORCE PASS değil; Q yazılı kararı: "tüm tenantlara bulksend aç").** `BulkSendOptions.AllowAllTenants` flag (ProjectsOptions pattern aynası; explicit flag, default false = davranış bit-identik) + prod config targeted-insert `BulkSend.AllowAllTenants=true` (.bak: `appsettings.Production.json.bak-20260610-bulksend-gateopen`) + Outbound deploy (~08:22, 10/10 HEALTHY). **YENİ GATE DURUMU: BulkSend=TÜM TENANTLAR (CSV/list bulk-send herkese açık, bridge route, mevcut hard cap'ler [MaxRecipientsPerCampaign config'teki değer]); Projects=[5050] (proje run + test-send dual-gate gereği SADECE 5050'de CANLI — artık gerçekten gönderir!); CxapiSend=YOK → cxapi route + HSM hâlâ INERT (HSM preview/confirm INV-OB-085).** Codex süreci: iter0 evidence-gap FAIL → iter1 callsite kanıtları (12/12+4/4 ama CRITICAL paket-format: ≥5 VQ + INVARIANT proof pack istedi) → iter2 INVARIANT-1..5 pack → PASS. Plan `arch/plans/20260610-bulksend-allowalltenants.json`.
- **2026-06-10 GECE-3: PROJELER UI/UX PACK ✅ DONE (Codex 3-chunk PASS: c1 iter1 [CQ10 dblclick-bubbling gerçek bulgu → closest() guard], c2 iter0, c3 iter1 [CQ12 inline-hint FP corrected-premise + Auth VQ]; Q uyurken tam otonom, Q pre-approval: "sormadan hepsini sırayla yap" + "deploy yap en son").** 9 GR + 2 prod-DB veri işi: **(DB)** 5050 "liste 1" 7 mojibake isim onarıldı (id-pinned `convert_from(convert_to(name,'LATIN1'),'UTF8')`, RETURNING 7/7; rollback `c:/tmp/mojibake-rollback-5050.sql`) + 15 gerçekçi TR mock liste / 192 kayıt seed (tek tx + verify; teyit: 17 liste/203 kayıt, sayaçlar tutarlı). **(UI/Backend)** CSV decode strict-UTF8(fatal)→win-1254 fallback (feature-detect) · wizard kolon tekil seçim + duplicate guard · "Son İçe Aktarmalar" kaldırıldı · 17 overlay/14 dosya dış-tık kapatma (onMouseDown+target guard, X handler'ı aynen; dirty/saving/creating guard'ları korunur) · değişken dropdown %doluluk eki kaldırıldı · "Gönderim Ayarları" başlığı silindi · proje modal max-w-6xl + lg 3-kolon (önizleme+param eşleme 3. kolon, plain_text baloncuğu) · **TEST-SEND**: Outbound `POST /api/v1/projects/send/test` (id'siz, create-mode test) + proxy + UI; gate sırası Projects→BulkSend→throttle(5/dk **INV-OB-087**/429)→PhoneNormalizer/4096(**INV-OB-086**/422)→CreateBroadcastAsync tek-alıcı inline-text (opt-out/consent/KVKK/route AYNEN reuse) + audit log; HSM projede render önizleme metni DÜZ METİN gider (Q kararı, UI notu) · `includeArchived` param (parametrik SQL, default false) + durum chip filtresi (Tümü=arşivsiz, sayaçlı) + çift-tık edit (arşivli hariç). **PROD-INERT: allowlist kesişimi ∅ → test-send 403; hiçbir gate'e dokunulmadı, P0-3 korundu.** Plan `arch/plans/20260610-projeler-uiux-pack.json` (plan-consult 12 bulgu/7 uygulandı). Commit `8ae8612d`. **✅ DEPLOYED (~05:20, Q onaylı "deploy yap en son"): migration YOK → Outbound → Backend+SPA `index-BFWwejdz.js`; 10/10 HEALTHY; gate snapshot DEĞİŞMEDİ (Projects=[5050] ∩ BulkSend=[18173130]=∅, CxapiSend YOK); test-send endpoint smoke: JWT'siz 401 (registered+gated, prod-inert).**
- **2026-06-10 GECE: PR-4 HSM APPROVED-TEMPLATE SEND ✅ DONE+DEPLOYED (commit `6c7d619d`; deploy ~03:57: migration 062 prod [INV-SEED-062 + read-back 6/6 kolon + 3-yol XOR + 2 CHECK] → Outbound → Backend+SPA `index-C9I5croP.js`; 10/10 HEALTHY; gate snapshot DEĞİŞMEDİ: Projects=[5050] ∩ BulkSend=[18173130]=∅, CxapiSend section YOK → P0-3 korundu; send/preview + wa-templates JWT-gated smoke OK) (Codex 5-chunk: 2/4/5 PASS iter0, 3 PASS iter2, chunk-1 Q FORCE PASS [tek kalan CQ12 = client caller-contract ArgumentException pre-existing PR-2/S3 shipped pattern; 11/12 CQ + 3/3 CoVe]; build 0E, 54/54 targeted test, 30 ortamsal fail master-baseline'da AYNI — stash kanıtlı sıfır regresyon).** Kapsam: **(1)** Migration **062** bulk_send_jobs +6 kolon (instance_id/template_kind/wa_template_id/param_mapping/template_language[display-only — INMA: send body'de language YOK, dil slug'a gömülü]/preview_skipped_params capped audit) + chk_bulk_message_source 3-yol XOR + chk_bulk_hsm_consistency + INV-SEED-062 + canonical outbound.sql senkron (PR-1 CQ11 borcu kapandı). **(2)** WapCrmSendClient.SendTemplateAsync — wire pinli: template{templateId, parameters[{paramKey,value}], headerMedia{url,fileName}}, RAW URL validation (pre-trim yok), fileName sanitize, SendCoreAsync ortak çekirdek (async faulted-task paritesi korundu). **(3)** Proje run HSM zinciri: CxapiSend allowlist gate VENDOR ÇAĞRISI ÖNCESİ preview+confirm'de (INV-OB-085; Codex plan-consult bulgusu — boş allowlist'te vendor'a tek çağrı yok, test pinli) → canlı template-list HARD-FAIL (081 slug-yok/082 required-param/083 dinamik-BUTTON [G13.3 ertelendi]/084 katalog-erişilemez) → ParseHsmMapping STRICT kind/source domain (bilinmeyen değer → 073, tahmin yok) → resolved-param snapshot (variables_json FLAT dict; çift telefonda ilk-hedef-liste+en-düşük-id deterministik; eksik-zorunlu-param alıcı snapshot DIŞI + preview_skipped_params audit + SPA amber uyarı) → confirm'de İKİNCİ canlı doğrulama JOB SNAPSHOT'ından (TOCTOU) + kind-mismatch re-preview reject → BroadcastOrchestrator HSM üretimi (message_kind='wapcrm_template', template_ref/template_params/template_header_media [PR-1 reserved ilk yazım], display placeholder '[WAPCRM_TEMPLATE] {slug}', KVKK-append bilinçli yok [şablon içeriği Meta-onaylı], opt-out/consent AYNEN) → MessageSenderService template dalı (pre-parse CAS öncesi, ArgumentException typed-catch, PR-3a state machine AYNEN). Kapsam SADECE proje run'ları. Plan `arch/plans/20260610-cxapi-pr4-approved-template.json`.
- **2026-06-10: VERİ-IMPORT TELEFON SCIENTIFIC FIX ✅ DONE+DEPLOYED (`b7db8329`, Codex PASS iter0, 12/12 CQ + 4/4 CoVe; deploy: Outbound+Backend+SPA `index-D1Lkc41z.js`, 10/10 HEALTHY, gate snapshot DEĞİŞMEDİ).** Excel'de SAYI-formatlı telefon SheetJS `raw:false` ile GÖRÜNTÜ metni döndürüyordu (`9.05332E+11`) → önizleme bozuk + import'ta PhoneNormalizer digit-strip `+90533211` (8 hane, Guard 8..15 GEÇER) = sessiz-bozuk-sendable riski. Fix 2 katman: **(1)** DataImportPage parse-anı w-repair (SADECE xlsx/xls + pozitif tamsayı + onarım `^\d{8,15}$`; **CSV'de ASLA** — SheetJS CSV'deki scientific METNİ de sayı hücresine çevirir ama v=905332000000 kayıplı, repair sahte numara uydururdu, repro kanıtlı) + `normalizePhonePreview` scientific reject aynası + tüm-satır tracked-boolean amber ipucu ("kolonu Metin yapıp dosyayı yeniden oluşturun"); **(2)** PhoneNormalizer `ScientificNotationRegex` reject (trim sonrası, digit-strip ÖNCESİ — import + bulk-send CSV `CsvRecipientParser` merkezi korunur). Node repro 16/16 PASS (`c:/tmp/xlsx-phone-repro.js`). Prod DB read-only tarandı: mevcut bozuk kayıt YOK. Plan `arch/plans/20260609-import-phone-scientific-fix.json`.
- **2026-06-09 GECE-2: aha pack 1-5 + liste görüntüleyici ✅ DONE+DEPLOYED (`82c7540e`, Codex PASS iter0 — 12/12 CQ + 4/4 CoVe). AYNI DEPLOY SS-D'Yİ DE CANLIYA ALDI: migration 061 prod'da çalıştı (INV-SEED-061 exception'sız + read-back 6/6 ✓) → Outbound → Backend+SPA (`index-BWMvng0S.js`), 10/10 HEALTHY, gate snapshot DEĞİŞMEDİ (Projects=[5050] ∩ BulkSend=[18173130]=∅, CxapiSend yok → P0-3 korundu).** İki READ-ONLY endpoint (mevcut ContactList gate arkasında, ContactList INV kodları reuse, yeni kod yok): `GET /api/v1/data-lists/preview-sample?listIds=` → {sample: ilk listenin ilk sendable kaydı, reach: COUNT(DISTINCT phone) sendable, column_stats: kolon doluluk} + `GET /api/v1/data-lists/{id}/records?search=&page=&pageSize=` → server-paged ILIKE arama (statik parametreli SQL, LIKE-escape, clamp'ler, 404 ownership probe) + 2 Backend proxy (query-string verbatim). **Projeler modal aha'ları:** #1 placeholder→kolon auto-match (COLUMN_ALIASES ad→name/email/alan1...) · #2 örnek-alıcı önizleme toggle'ı (baloncukta gerçek değerler yeşil; debounced+seq-guard+FAIL-SILENT) · #3 kolon dropdown'unda örnek değer + %doluluk · #4 başlıkta dedup'lı "X benzersiz alıcı" · #5 dirty-form X-close onayı (fingerprint baseline async edit-load SONRASI). **Veri Yönetimi:** liste satırına çift-tık/göz ikonu → X-close viewer popup (tablo + boş kolon gizleme + gönderilemez rozeti + debounced arama + Önceki/Sonraki sayfalama). Plan `arch/plans/20260610-projeler-aha-list-viewer.json`. Paralel-session koordinasyonu sorunsuz: diğer session'ın SS-D'si bitene kadar beklendi, pull sonrası güncel base'e kuruldu.
- **2026-06-09 GECE: SS-D ✅ DONE+COMMITTED `c98b03fc` — ✅ DEPLOYED (2026-06-09 GECE-2 deploy'u ile: migration 061 + Outbound + Backend canlı; pause/resume/cancel endpoint'leri artık prod'da, BulkSend gate'i sayesinde inert).** Pause/resume/cancel run lifecycle: migration **061** (chk_message_status +'paused' pre-terminal +'cancelled' terminal, chk_bulk_job_status +'paused', outbound_broadcasts.cancelled sayacı, projects.cancelled_count, INV-SEED-061) + ProjectsRepository Pause/Resume/CancelRunAsync (tek-tx atomik status claim → typed 404/409 INV-OB-077..079, asla silent no-op; pause=queued→paused flip [worker sadece 'queued' claim eder → doğal halt, in-flight 'sending'/'posting' geri çağrılamaz], resume=paused→queued, cancel=tek atomik CTE queued+paused→cancelled + per-broadcast cancelled++/queued-- + drained broadcast AYRI same-tx komutla complete) + RecomputeRollup cancelled_count + paused/cancelled status+completed_at koruması + IsBroadcastCompleteAsync 'paused' non-terminal + **one-active-run** (INV-OB-080: preview running/paused reject; confirm'de yalnız fresh `preview_ready` dispatch reject, idempotent re-confirm muaf) + 3 endpoint/proxy `send/pause|resume|cancel` + ProjectsPage durum-duyarlı kontroller (Duraklat/Devam Et/İptal + X-close iptal onay modalı + 'N iptal' rozeti). Codex gpt-5.5 iter0 FAIL(4: CQ5 SQL-interpolation, CQ9/Q3 tenant-blind, Q5 confirm-guard deliği) → iter1 fix → 11/12 CQ + 4/5 CoVe; tek kalan CQ9 = IsBroadcastCompleteAsync pre-existing tenant-blind (broadcast_id UUID PK, PR-3a sanctioned worker sınıfı) → **Q FORCE PASS**. **Deploy sırası: ÖNCE migration 061, SONRA Outbound+Backend+SPA** (Backend `SELECT p.cancelled_count` migration'sız kırılır).
- **2026-06-10 (Backend+SPA, CLIENT-ONLY, 2 deploy, ikisi de Codex PASS):** **(1) wa-templates 502 FIX** (`b28073db`) — canlı cxapi `POST /api/templates` `preview` alanı OBJE (`{header,body,footer,buttons}`), `string?` değil → JsonException → TransportError → 502 (tenant 5050/instance 6570). Fix: `WapCrmTemplatePreviewDto` + name/language/category/paramFormat + gerçek-wire-shape pinli parse testi. **(2) Projeler modal REDESIGN** (`4de49e02`) — iki-kolon (sol ad/açıklama/liste, sağ gönderim) + kanal etiketinden (kod) kaldırıldı + Onaylı Şablon default/leftmost, Düz Metin disabled, Yok kaldırıldı + WhatsApp-stil önizleme baloncuğu (`{{...}}` vurgulu) + her placeholder (header'daki **hname** dahil — önizlemeden regex'le çıkarılır, cxapi requiredInputs eksik bıraksa bile) için liste-kolonu dropdown (Ad/Soyad/E-posta/Alan1-5/Etiket/Not). **Şablon OPSİYONEL** (seçmezse metadata-only kaydedilir). `param_mapping` entry shape += `source:'column'/column` (opak JSON, server `BuildSendConfig` sadece object/array+≤16KB doğrular → backend değişmedi; **PR-4 tüketir, şu an INERT**). SPA bundle artık `index-Bag-9o6R.js`. P0-3 dokunulmadı.
- **2026-06-09 PM: SS-A/SS-B/SS-C PROD'A DEPLOY EDİLDİ.** Migration **059+060** + Outbound + Backend + SPA bundle (`index-L_L4TiDm.js`) canlı. **10/10 service HEALTHY.** Her iki migration INV-SEED verifier PASS; read-back doğrulandı.
- **⛔→✅ P0-3 GATE GÜNCEL DURUMU (2026-06-11 AKŞAM-5 itibarıyla):** `BulkSend.AllowAllTenants=true` (TÜM tenantlar, bridge route) + `Projects`=[5050, 15702882, 100000001] (5050 + Medipol'ün iki tenant satırı — 2026-06-11 gate-open) + **`CxapiSend`=[5050, 100000001]** (cxapi route + HSM: 5050 + Medipol-wappflex [akşam-5a provisioning: wapcrm config CANLI — secret + user_id=1 + instance 8784 WABA]; **Medipol ack webhook ✅ SET 2026-06-12 cxapi self-serve API ile (`/api/webhook-settings/messages`, companyId=100000001) — yeni gönderimler delivered/read izlenir**; mlpcm 15702882 cxapi'siz [ApiStatus=false]; başka tenant için cxapi runbook: instance backfill + allowlist + webhook (artık API ile)). **2026-06-13: webhook SET artık OTOMATİK — FEATURE A `CxapiWebhookReconcileJob` ENABLED (AllowAll) prod'da, tüm WapCRM-konfigli tenant'ların ack webhook'unu periyodik hizalıyor (manuel API çağrısı gereksiz; yabancı URL'lere dokunmaz).** Eski not (tarihçe): 2026-06-09'a kadar Projects=[5050] ∩ BulkSend=[18173130]=∅ idi.
- **Kod (commit'li, hepsi Codex PASS — FORCE PASS değil):** SS-A (`ed641ad8` broadcast inline-text), SS-B (`5f61a077` proje içerik config: gallery_template VEYA free_text), SS-C (`7c82d621` proje run dispatch + Gönder UI). Mimari: bir run = `bulk_send_job(project_id)` → mevcut bulk preview→confirm→status makinesi reuse.
- **Daha önce canlı:** PKT-14 S1-S4 (migration 055+056+057, 2026-06-07) + cxapi PR-3b-1/PR-3b-2 (migration 058 delivery-ack ingress, 2026-06-08). Tüm cxapi send/ack kodu prod'da ama **inert** (external_message_id NULL → ack 404→202-swallow).

**Pilot tenant gerçeği:** WapCRM-konfigli TEK tenant = **5050 (TestEticaret)**, vendor sandbox, instance_id NULL → **gerçek pilot YOK**. Gate-open için gerçek müşteri tenant + instance_id gerekli (Q kararı). Codex = gpt-5.5, LOW dahil her risk review.

**Working tree durumu (2026-06-11 akşam-4):** KİRLİ — eşzamanlı session'ların DEPLOY EDİLMEMİŞ + kısmen review'suz WIP'i var (Outbound/Shared list-record-add-remove + projeler-waba-only-dropdown + Backend Program.cs endpoint'leri); committed-ama-deploy-edilmemiş: TR-sweep `c8a20e04` + `e516a1df` + nav paketi `60c32cd3`/`7966d4b5`. **Q KARARI (akşam-4 wrap): tüm açık session'lar kapanınca TOPLU DEPLOY yapılacak — tek tek deploy YOK.** SONRAKİ (toplu) DEPLOY bunların hepsini taşır — deploy öncesi `git status` temiz + `git log origin/master..HEAD` + bekleyen migration taraması (063 + backfill sırası akşam-5b'de) ZORUNLU. **Paralel-session dersi (2026-06-11, GÜNCELLENDİ):** aynı master tree'de 3+ session eşzamanlı; pathspec-scoped commit yetmedi — working-tree'den commit eden sweep başka paketin edit'lerini süpürdü, eski-HEAD-bazlı cerrahi blob ise sweep'i kısmen revert etti (fix-forward `7966d4b5`). Kural: cerrahi commit'te blob bazı == commit-anı HEAD doğrula + update-ref HEP CAS'lı + commit sonrası stat'ı beklenen satır sayısıyla karşılaştır.

## Execution Queue (açık/pending — master: tracking/pilot-launch-roadmap.md)

- **FEATURE C — RECOVERY SWEEP 'sending' KAPSAMI: ✅ DONE+DEPLOYED (2026-06-13, commit `f709989d`, Codex PASS iter2 12/12 CQ + 4/4 CoVe).** Migration **064** (claimed_at TIMESTAMPTZ + partial index `idx_outbound_messages_cxapi_sending` + INV-SEED-064) prod'da çalıştı, kolon doğrulandı. dequeue `claimed_at=NOW()` + `OutboundRepository.SweepStrandedSendingAsync` (periyodik 'sending'→'queued', claimed_at staleness-gated) + `CxapiSendOptions.StaleSendingMinutes=3` + `TryRecoverStrandedAsync` sweep çağrısı (own try/catch→INV-OB-096). Counter-neutral. **Duplicate-safety = 2 bağımsız garanti:** single-flight `_isProcessing` (sweep send'lerle serialize) + posting CAS (`SetMessagePostingAsync` reset olmuş satırda POST'u atlar → çift gönderim imkansız). Codex iter0 CQ9/Q1/Q2 false-positive'di (posting CAS diff dışıydı → koda kanıt yazıldı), iter1 yalnız VQ-coverage Tenant/Auth nit'i. Outbound deploy ile canlı (`MessageSenderService starting`).
- **FEATURE A — AUTO-WEBHOOK-SET ✅ DONE+DEPLOYED+ENABLED (`6df65d7e`, Codex PASS iter1; 2026-06-13 FEATURE C ile beraber deploy + ENABLE AllowAll).** Outbound `CxapiWebhookReconcileJob` (Timer IHostedService, InmaOptOutSyncJob deseni) + Shared `WapCrmWebhookSettingsClient` (GET /api/webhook-settings + POST /api/webhook-settings/messages; per-request X-CIB-SecretKey; 509=IpDenied, 301/302=RateLimited; typed outcome, throw YOK). Startup+15s ardından periyodik (10dk) sweep: secret'lı tenantları tara → boş/bizim-ama-pasif/eski-owned-host → SET; **YABANCI URL = DOKUNMA + warn INV-OB-092**; `/events` HMAC kanalına dokunmaz; cache YOK. **PROD CONFIG: `CxapiWebhookReconcile {Enabled:true, AllowAllConfigured:true}`** (Q kararı AllowAll; backup `bak-20260613-cxapi-webhook-reconcile`). INV-OB-091/092/093. Canlı log doğrulandı: `allowAll=True, intervalMs=600000, publicBase=services.invekto.com`. ⚠️ İZLE: ilk sweep'ten sonra tenant'ların webhook'u set oldu mu + 509 (IP whitelist) hatası var mı (server-logs INV-OB-093). Plan `arch/plans/20260612-cxapi-webhook-auto-reconcile.json`.
- **FEATURE B — RAPOR STATUS-PULL ✅ DONE+DEPLOYED + BATCH REWORK ✅ DONE+DEPLOYED (`63e9ef76`, Codex PASS iter0 12/12+3/3, 2026-06-12 son session). CANLI ŞEMA DOĞRULANDI (Medipol WABA 8784 messageStatus=3).** Batch rework: tek-tek `GetMessageStatusAsync` yerine `GetMessageStatusesAsync` (instanceID + messageIDs[] ≤100, typed `data.messages[]` parse: found+messageStatus 0-5); caller resolved-instance grupla/chunk≤100/apply (ApplyDeliveryStatusAsync değişmedi). 626=graceful Ok=true boş; 506/404/transport=Ok=false best-effort. Singular+ExtractMessageStatus korundu (Q, breaking yok). Düzeltilen defect: singular parse top-level `data.messageStatus` arıyordu (canlı `data.messages[]` altında nested) → buton inert'ti. Önceki 401'ler=PowerShell curl gövde mangling artefaktı (C# HttpClient sağlam). Plan `20260612-cxapi-message-status-batch.json`. ↓ Orijinal (iter1, `82e4f596`): Rapor drawer'da **"Durumu Yenile"** butonu → Outbound `POST /api/v1/projects/{id}/report/refresh-status` (+ Backend proxy): bekleyen wamid'li alıcıları (status IN sent/delivered/ambiguous, external_message_id NOT NULL, 200 cap) çek → her biri için cxapi `POST /api/message-status` ({instanceID, messageID=wamid}) → **AYNI** `WapCrmAckMapping.MapStatus(0-5)` (ack webhook'uyla ortak) → idempotent monotonic `ApplyDeliveryStatusAsync` ile uygula → `{checked, updated}` döndür. Shared `WapCrmSendClient.GetMessageStatusAsync` (per-request secret, finite timeout, NO retry — best-effort) + `WapCrmMessageStatusResult`; **message-status response şekli DOKÜMANTE DEĞİL → defensive parse** (data-as-number VEYA data-as-object messageStatus). Gate: **CxapiSend allowlist** (INV-OB-094 → allowlist dışı = sıfır vendor çağrısı, P0-3 inert; INV-OB-095 db). Plan `arch/plans/20260612-projeler-report-ux-statuspull.json`. **KALAN: deploy + canlı message-status response şeması doğrulama (allowlist'li tenant ile).**
- **MEDIPOL-WAPPFLEX-VERIFY: ✅ VERIFIED (2026-06-11 akşam):** SSO uçtan uca çalıştı — tenant 100000001 lazy-provision oldu (16:17, deploy'dan 1 dk sonra; updated_at 16:48 = girişler sürüyor). **YENİ GERÇEK:** wappflex token'ı `medipol` company code taşıyor → mlpcm 15702882'ye ÇÖZÜLMEDİ, AYRI tenant oluştu (beklenti "ayrı satır gerekmez" idi — yanlıştı). İki satır = iki ayrı veri alanı (proje/liste/mesaj verisi portala göre ayrışır). Q isterse INMA'ya sorulacak: "wappflex token'larında company code neden mlpcm değil medipol?" — tek tenant'ta birleştirme ancak INMA tarafında code eşitlenirse mümkün.
- **GATE-OPEN (kalan kısım — GERÇEK MÜŞTERİ pilotu): Q kararı 2026-06-10 = header-fix beklenecekti → FIX AYNI GÜN GELDİ (Bug1 verified; Bug2 canlı test kaldı).** BulkSend ✅ tüm tenantlar. CxapiSend ✅ [5050]. **5050 DELIVERY-ACK LOOP AÇILDI ✅ (2026-06-10 15:45):** INMA, 5050 webhook URL'ini `https://services.invekto.com/api/v1/webhook/event?companyId=5050` yaptı (MSSQL CompanyWebhookEndpoints kanıtlı; webhook.site devri bitti) → ack'ler artık bize düşer, Meta 131049 retleri görünür olur. Gerçek müşteri için KALAN: Bug2 canlı doğrulama + tenant allowlist + WapCRM config + o firmanın webhook URL yönlendirmesi.
- **INMA-CXAPI-HEADER-FIX: TAMAMEN KAPANDI ✅✅ (2026-06-10 — rapor sabah iletildi, INMA AYNI GÜN iki bug'ı da fix'ledi, akşam canlı doğrulandı):** Bug1 (requiredInputs) canlı template-list çekimiyle verified (şablon 1664033448202974 → `[{text@HEADER:hname},{text@BODY:name}]`); Bug2 (chatoperation header uygulama) Q'nun canlı test-send'iyle verified — alıcıda **"Test taner"** (msg id=14, wire `{name:taner,hname:taner}`). Amber uyarı kendiliğinden kayboldu (tasarım gereği). Guide güncel kopyası: `arch/docs/wapcrm-api-integration-guide.md` (HEADER location + YENİ `fixedValues` kavramı — sabit atanmış alanları parameters'a KOYMA).
- **WEBHOOK-401 FIX (2026-06-10 ~18:00 TR, config-only):** INMA'nın YENİ dispatcher'ı (ack+inbound+system-events, kayıtlı URL'e) `78.135.105.53`'ten (hayalhost) POST ediyor; `Webhook.AllowedIps`'te yalnız eski `91.151.84.79` vardı → HER push 401 INV-AUTH-003 (INMA 401'de max 3 retry → kalıcı fail; bugünkü ack'ler bu yüzden kayıp, mesajlar 'sent'te kaldı; Q'nun inbound'ları da reddedildi). **Fix (Q onaylı):** AllowedIps += 78.135.105.53 (.bak: `bak-20260610-webhookip`) + Backend restart, health OK; sonraki Backend deploy'u config-restore'lu yapıldı (IP korunuyor, doğrulandı). IP teyidi PTR+davranış-temelli (INMA yazılı teyidi YOK — Q isterse sorar). Fix ÖNCESİ ack'ler 401-retry'larını tüketti = KAYIP (bilinen). **KALAN KANIT: ilk 202** — Q'nun yeni inbound mesajı veya yeni test-send ack'i ile; ack'le status sent→delivered ilk geçişi de aynı anda doğrulanır. NOT: `customer.selection_changed` event'leri C2 dalı eklenene dek 400 alır (INMA'da kalıcı fail — C2 önceliği).
- **KARAR-INMA-PIPELINE-CONTRACT: RESOLVED ✅ (2026-06-10, AYNI GÜN) → C2/C3/C4 UNBLOCKED.** Teklif iletildi, INMA cevapladı (`arch/reports/20260610-inma-customer-status-webhook-cevap.md`): event ZATEN CANLI (`customer.selection_changed`, AYRI sistem-olayları altyapısı — HMAC-SHA256 `X-Invekto-Signature` 'timestamp.body' + transactional outbox + at-least-once retry [anlık→1dk→5dk→15dk→1sa→6sa, 6 deneme; 4xx KALICI fail]); INMA aynı gün ekledi: `data.phone/phones` + `originRequestId` echo (update'e `ClientRequestID` max128) + text-modu. ChangeVersion YOK (Faz 2) → fallback: occurredAt guard + `before` zincir kontrolü + customer-selections fetch-verify (`before` HEP dolu). **ABONELİK KURULU (MSSQL kanıtlı):** 5050 endpoint Url=bizim webhook + EventType=customer.selection_changed Enabled. Nihai kontrat: `arch/contracts/inma-customer-status-webhook.json` **v2.0 FINAL** (INMA gerçek şeması camelCase + consumer iş listesi). ⚠️ **C2 ZAMAN-DUYARLI:** handler'da `type` dalı yokken eventler 400 alır = INMA'da kalıcı fail. C2 ek şartları: HMAC imza doğrulama ZORUNLU (secret Q panel'den alacak) + telefon ülke-kodu garantisiz (phones[] eşleme + customerId fallback).
- **FEAT-PIPELINE (FEAT-INMA-PIPELINE-V2): C2 ✅ + C3a ✅ DONE+DEPLOYED + C3b ✅ DONE-COMMITTED+PUSHED (Codex PASS iter0; DEPLOY HELD — Q server-deploy'u reddetti, "devam" dedi → deploy+smoke Q'da).** C3a backend trigger borusu CANLI. **C3b (`08cbb9c2` feat + `4e637124` spa + `2ad5058b` ttl, origin/master'da `2ad5058b`):** SPA `customer_status_changed` trigger node (webhook_trigger aynası: flow.ts union/registry, graph-validator SOURCE_NODE_TYPES, node-metadata NODE_GUIDES[exhaustive]/NODE_OUTPUT_VARS, NodePalette NODE_ICONS[exhaustive], nodes/index, CustomerStatusTriggerNode.tsx, NodePropertyPanel CustomerStatusChangedProps) + **Backend-direct 1h-cached cxapi katalog proxy** `GET /api/v1/customer-feature-groups` (+`/cache-invalidate`) → feature_group_id picker. Yeni Shared `WapCrmFeatureGroupCatalogClient`+`Cache` (WapCrmTemplateClient+InmaDynamicFieldsCache aynası; per-request X-CIB-SecretKey, SSRF-fixed base, throw-on-failure, single-flight, failures-not-cached). **Single-trigger-slot fix** (flow-store.addNode trigger drop'ta mevcut trigger'ı REPLACE eder — her flow undeletable trigger_start seed eder, yoksa node kullanılamaz). node.data.feature_group_id numeric STRING (`''`=catch-all, C3a GetData read-path; non-numeric sessizce skip → picker yalnız `''`/sayısal yazar). Metin-modu (selectionMode=3) gruplar disabled+onChange-guard. WapCRM'siz tenant=catch-all+not. INV-BE-132/133. **No migration.** Additive Shared → diğer 10 servis inert (Backend-only deploy yeterli). **DEPLOY ARTIFACT HAZIR:** `deploy_output/Backend` published (Invekto.Backend.dll + SPA `index-E1nc-dJ3.js`); baseline 10/10 HEALTHY. **Smoke (AC10, deploy sonrası):** 5050 hand-seed customer_status_changed flow (SQL) + uçtan uca tetik; Medipol secret'a dokunma (Q kararı); ayrıca prod server-side cxapi catalog curl (5050 secret) ile wire-doğrula. **Sıradaki: deploy(Backend) + smoke → C4** Set Customer Status write-back (INMA update endpoint + `invekto-{flowRunUuid}` ClientRequestID, `CustomerStatusFlowSuppression.OriginRequestIdPrefix` reuse). **C3 hardening backlog:** bounded-retry+idempotency-table+outbox (AutomaticRetry=0 lossy). **C3b/C4 prereq:** Medipol (100000001) `webhook_secret` set değil.
- **UP0.3** Tenant lifecycle handler: PENDING (INMA tenant.created event bekliyor). **UP0.5** IInmaSendClient: PENDING (INMA J1/J4 bekliyor).
- **PKT-13 Faz 1** Lead Scoring: PENDING (Marketing). **FEAT-OBI Faz 2:** telefon tek-numara geçmiş arama (Faz 1B DONE+DEPLOYED).
- **FEAT-META-CAPI / META-ADS-INSIGHTS:** DRAFT (Q provision bekliyor: Pixel/token/App Review). META-MARKETING-API: BACKLOG ($50k+/ay gate).
- **Dent Adavista pilot:** BLOCKED (UP0 + FEAT-* sonrası). **Zoho OAuth smoke** (5050 e2e): DEFERRED (INMA creds 401, opsiyonel).

<!-- END_CURRENT_STATE max_lines=300 -->
