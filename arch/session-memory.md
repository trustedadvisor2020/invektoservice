# Session Memory — Current State

> **BOUNDED FILE.** OVERWRITE edilir (append değil), ≤300 satır. Session init `limit=320` ile okur; sonundaki `END_CURRENT_STATE` işareti o pencerede görünmüyorsa DUR → `/optimize-memory`. Eski tam log: [session-memory-archive.md](session-memory-archive.md) (SADECE Grep). Kalıcı kararlar: [docs/decisions.md](docs/decisions.md). Standing kurallar: [hot-lessons.md](hot-lessons.md).
> **Pilot Mode:** queue+tracking otoritesi `tracking/pilot-launch-roadmap.md`'de; bu dosya son durum detayı.

_Güncel: 2026-06-15 — **AKTİF İŞ: Refactor Audit CONSOLIDATION DONE.** 18 unmerged audit branch'i (**6 DEĞİL** — narrative/session-memory count yalnız latest-wave'i sayıyordu; gerçek = `git branch --no-merged master`) tek integration branch'te toplandı: **`work/20260614-audit-consolidation`** (master `77aa6fda`'dan; **LOCAL + remote push'lu**; **MASTER'A MERGE YOK, DEPLOY YOK** = Q kararı, "wrap = close, not merge"). Merge sonucu: **kod CONFLICT-FREE** (`git merge-tree --write-tree` ile non-destructive önceden doğrulandı — tüm `.cs`/`.ts` auto-merge clean dahil Backend Program.cs settings∩payment, getint32∩sweep repo'ları); tek çakışma `arch/codex-context.md` (8 branch additive sanctioned-broad-catch append'i, marker-strip auto-union, her branch'in eklediği satırlar line-by-line doğrulandı = **0 missing**) + `session-memory.md` (--ours). **Full-solution build = 0 error** (42 warning = pre-existing test-proj MSB3277/xUnit). **DEPLOY GATE = GREEN (Q-onaylı prod-config yazımı):** Outbound `InternalServices:SharedSecret` = Backend değeri (sha8 04A96F3D, MATCH — Outbound prod override hiç yapılmamış, 36-char base default kullanıyordu) + WAA `Benchmark:OpsKey` yeni 64-hex random (sha8 EEB34235, server-side, eksikti → restart'ta WAA'yı düşürürdü); ikisi prod `Production.json`'a targeted-insert + `.bak-20260614-auditgate`, **on-disk only, restart/deploy'da etkin**. Per-service fail-closed gate her servis TEK key gate'ler: ChatAnalysis=InternalApiKey (VAR), WAA=Benchmark:OpsKey (yeni set). Plan + tam detay: **`tracking/audit-consolidation-plan.md`**. Önceki teslimler: WebChat §7 (`7d895d47`) + Backend auth/login (`30fd9361`) = MERGED+DEPLOYED (tek deployed audit batch'leri); diğer 18 branch unmerged → şimdi consolidation'da. FEAT-INMA-PIPELINE-V2 C3b DONE+DEPLOYED (`633c07bd`). Tam tarihçe → session-memory-archive.md (grep)._

## AKTİF İŞ — Refactor Audit 2026-06-14

**Giriş:** `arch/reports/20260614-audit-INDEX.md` (master) + memory `project_refactor_audit_2026_06_14`. TÜM .NET kod tabanı read-only tarandı → **176 bulgu (8 Critical / 34 High)**, triage Q'da. Hiçbir şey master'a merge edilmedi, deploy yok.

**Çalışma kuralı (HER batch):** master'dan yeni `work/` branch → auto+Codex PASS → **commit'te DUR** (deploy YOK, master'a merge YOK) → auth/Shared değişikliğinde **MUST-ASK**. Branch'leri master'a merge + deploy kararı Q'da.

**Batch durumu:**
- ✅ **Batch 1** — GetInt32-on-bigint `::int` cast: `work/20260614-getint32-bigint-cast-fix` (`ad7efb63`), remote push'lu, merge YOK. (NOT: Knowledge `TemplateExtractorService.cs:334` COUNT(*) GetInt32 crash'i de KAPSADI.)
- ✅ **Batch 2** — INV-BE errorcode collision (6 çakışma → 134-139): `work/20260614-errorcode-collision-fix` (`0c3c3a4a`), push'lu, merge YOK
- ✅ **Batch 3** — fail-closed auth (ChatAnalysis+WAA startup-throw): `work/20260614-failclosed-auth-fix` (`82424043`), push'lu, merge YOK. **DEPLOY PREREQ:** prod'da `Benchmark:OpsKey` + `Microservice:InternalApiKey` set et.
- ✅ **Batch 4** — VoiceAI path crash+traversal (`VoiceTranscriptionService.cs:42` `:`-crash + CWE-22 traversal → sanitize + containment guard): `work/20260614-voiceai-path-crash-traversal-fix` (`c23c9831`), push'lu, merge YOK. Codex PASS iter0 (MEDIUM).
- ✅ **Batch 5** — Outbound internal-endpoint: **GERÇEK bug teyit edildi** (opt-out + outbox-drain FEAT-J2'den beri 401-kırık; Backend JWT mint etmiyor). Fix A (Q onaylı): `Program.cs:301` JWT exclusion `/api/v1/internal/`. `work/20260614-outbound-internal-jwt-exclusion-fix` (`fff607fd`), push'lu, merge YOK. Codex PASS iter0 (HIGH). **DEPLOY PREREQ:** Backend+Outbound prod `InternalServices:SharedSecret` set+eşit.
- ✅ **C4 (orphaned critical, batch-dışı)** — Knowledge `GetPublishedForComparisonAsync` SELECT'i `group_tag`'i (ordinal 21) projekte etmiyordu → `ReadCatalogDto` IndexOutOfRangeException (her yayınlı faq/intent template'inde, TemplateExtractor'dan erişilir). Fix: inline 21-kolon listesi → canonical `CatalogSelectColumns` (projection-only, ordinal 0-20 byte-identical). `work/20260614-knowledge-c4-grouptag-select-fix` (`2e7c8325`), push'lu, merge YOK. Codex PASS iter0 (MEDIUM). **Batch 1 sadece GetInt32-sınıfını kapatmıştı; eksik-kolon sınıfı çatlaktan düşmüştü → C1–C8 critical'lerin TAMAMI artık kapandı.**
- ⏭️ **Batch 8 — ATLANDI (FALSE-POZİTİF, Q kararı 2026-06-14).** wa_intents/wa_faq_clusters tenant-scoping bir izolasyon açığı DEĞİL: endpoint superadmin-only + `analysis_id` FK→`wa_analyses` tek-tenant'a bağlar + catalog global-by-design + audit "ZERO isolation". Bu metotlardaki tek gerçek bug = GetInt32 crash, o da Batch 1'de kapandı. tenantId threading redundant (aynı satırlar). Kod değişikliği YOK.
- 🔄 **Batch 6/7 (DEVAM — broad catch + null-forgiving sweep, ~150 med/low, çok-paket)** — İNCREMENTAL: servis-servis / endpoint-grubu bazında, rewrite DEĞİL.
  - ✅ **AgentAI DB-path (AI-1,2,3)** — 3 broad catch(Exception)→typed catch(NpgsqlException)+INV-DB-001; AI-1 OCE-rethrow guard; /feedback mesajı spesifikleştirildi. `work/20260614-agentai-dbpath-typed-catch` (`e30b4605`), push'lu, merge YOK. Codex **iter1** PASS (iter0 FAIL = generic /feedback mesaj → hot-lessons'a yazıldı). INV-DB-001 reuse (Shared'a dokunulmadı).
  - ✅ **AgentAI AI-gen-path (AI-4,5,6,7)** — broad catch(Exception)→typed (HttpRequestException/JsonException/NotSupportedException). AI-4/AI-6 = defensive `content[0].text` navigation (ValueKind+TryGetProperty guard) → catch typed kalır, graceful-null/raw-history korunur; genuine-unexpected propagate (AI-4'te 500). AI-5 = ParseResponse trailing catch→InvalidOperationException. AI-7 = /suggest summarization wrapper optional-step degradation boundary olarak KALDI, OCE rethrow ile narrow. `arch/codex-context.md`'ye "optional-step degradation boundary" sanctioned broad-catch kaydı eklendi. `work/20260614-agentai-genpath-typed-catch` (`1465dad1`), push'lu, merge YOK. Codex **iter0** PASS. Shared/error-code dokunulmadı.
  - ✅ **AgentAI AI-9 (header robustness)** — ReplyGenerator + ConversationSummarizer (x-api-key/anthropic-version) + KnowledgeHttpClient (Authorization Bearer): `Headers.Add`→`TryAddWithoutValidation`. FormatException (malformed config key/jwt) typed-catch setinde yok → graceful boundary'yi kaçırıyordu; artık throw etmiyor, davranış-koruyan. `work/20260614-agentai-header-tryaddwithoutvalidation` (`dd310f0e`), push'lu, merge YOK. Codex PASS iter0 (LOW). Auth header dahil = Q onayı.
  - ✅ **AgentAI AI-8 (perf-dup)** — KnowledgeHttpClient.ParseSearchResponse `results` array'ini İKİ kez enumerate ediyordu (Sources + ContextText) → tek pass'e birleştirildi. Davranış-koruyan (aynı sıra/guard, null=="" eşdeğer). `work/20260614-knowledgeclient-parse-singlepass` (`f27b0bf8`), push'lu, merge YOK. Codex PASS iter0 (LOW). **→ AgentAI §7 sweep TAMAM.**
  - ✅ **Knowledge error-handling sweep (K-2,5,6,7,9,10,12,13,14)** — broad-catch→typed sweep tüm Knowledge servisinde. K-2: 8 request endpoint broad→OCE-rethrow+NpgsqlException (2 ex.Message sızıntısı temizlendi). K-5: 9 zero-catch superadmin catalog/suggestion/adoption endpoint sarıldı → INV-DB-001 (generic-DB reuse); 7 handler'a JsonLinesLogger. K-6: 23505 sonrası NpgsqlException fallback. K-7: upload catch DB/IO split + typed cleanup helper. K-10: generate-embeddings → NpgsqlException only (HTTP zaten EmbeddingService'te null). K-9/12/13: sanctioned degradation/resilience broad-catch (RetrievalService fallback + DocumentProcessingService startup+worker loop), cancellation-first (throw/break). K-14: EmbeddingService Headers.Add→TryAddWithoutValidation. codex-context.md'ye sanctioned broad-catch politikası eklendi. `work/20260614-knowledge-errorhandling-sweep` (`a87b0a15`), push'lu, merge YOK. Codex PASS **iter1** (iter0 FAIL = (a) sanctioned-rule wording worker-loop'ta `break`e izin vermiyordu, (b) diff paste'i kesilmişti → false-signal). Shared/error-code dokunulmadı. **Kapsam-dışı kalan Knowledge bulguları:** K-3 (Sector="eticaret", davranış+migration), K-8 (UpdateAsync tx/TOCTOU), K-11/K-15 (dedup), K-16 (scenario→faq mapping).
  - ✅ **WhatsAppAnalytics error-handling sweep (WAA-1,2,5,6,7,9,10,11,12,13,14,17)** — 34 broad-catch sitesi tarandı. **Tier A** (request-path tek-konu DB handler): `catch(OperationCanceledException)`→499 + `catch(NpgsqlException ex)`→log+WADatabaseError, broad kaldırıldı (propagate→TrafficLoggingMiddleware). **Tier B sanctioned** (codex-context.md yeni bölüm): compute/mining POST (MSSQL+PG, SqlException ulaşılabilir→coded WAInsightComputeFailed korundu), upload (file+DB), request-body-parse (400), dashboard/onboarding aggregator, BulkOrchestration per-sector — hepsi OCE-first + ex.Message client'a SIZMIYOR (log-only). WAA-5/13/16 null-forgiving→guard; WAA-10/17 await using cmd (6 Upsert); WAA-11 dup SafeGet→shared InsightSafeGet. `work/20260614-waa-errorhandling-sweep` (`ab69be82`), push'lu, merge YOK. Codex PASS **iter2** (iter0 FAIL=read-path NpgsqlException loglamıyordu→10 handler'a jsonLogger; iter1 FAIL=BulkMineResult.Errors'a ham ex.Message→sanitize). Shared/error-code dokunulmadı. **WAA-3 OpsKey=Batch 3 sahibi (hariç). Ertelenen:** WAA-4 (SUM-vs-AVG GERÇEK bug, quality<100 satır 10x şişik), WAA-8 (ON CONFLICT latent), WAA-15 (DI dup).
  - ✅ **WAA-4 (SUM-vs-AVG gerçek bug)** — GetQualityInsightAsync groupByAgent=false dalı <100 satırda AvgOverallScore'u SUM olarak döndürüyordu (10x şişik, dominant path). **Audit'in one-liner'ı (totalOverall/scores.Count) REDDEDİLDİ** — ==100 dalında totalOverall zaten DB AVG'ye overwrite ediliyor, /scores.Count(=100) o dalı kırardı. Q-onaylı Option B (branch-aware): page için avg=SUM/count; ==100 dalında DB AVG direkt assign (bölme YOK) + totalCount=DB COUNT; return'de tek round. Consumers: RiQualityScore.tsx + OnboardingInsightService TenantAvgQualityScore. `work/20260614-waa-quality-avg-fix` (`6a7007ee`), push'lu, merge YOK. Codex PASS iter0 (MEDIUM). Tek dosya, Shared/error-code dokunulmadı.
  - ✅ **Backend settings/instances grubu (Backend-10,12,13,14,23,25)** — 3 endpoint sitesi broad catch(Exception)→typed. GET auto-fetch: 4 typed catch (Http/Task/Json/Npgsql) log-and-continue (sanctioned best-effort degradation boundary, cached list servis edilir). POST /refresh: typed + **GÜVENLİK (Backend-14)** ham ex.Message 502 body'den çıkarıldı→log-only+static mesaj; status by-class (502/504/502/503), BackendInstanceFetchFailed reuse. PUT toggle body-parse: bare catch→JsonException (working-hours sibling paterni). **TaskCanceledException = transport timeout (FetchWapCrmInstances token wire ETMİYOR, caller-cancel yok)** → codex-context.md'ye anti-FP not. `work/20260614-backend-settings-instances-typed-catch` (`ef88ec8f`), push'lu, merge YOK. Codex PASS iter0 (MEDIUM). Shared/errors.md/using dokunulmadı.
  - ✅ **Backend payment grubu (Backend-7, QNB 3DPay)** — 3 site broad catch(Exception)→typed. /initiate body-parse→JsonException; /initiate main→NpgsqlException (QnbVPosService.InitiatePayment PURE/no-HTTP teyit edildi — client-browser auto-submit HTML; audit'in HttpRequestException önerisi dead-catch'ti, reddedildi); /history→NpgsqlException (pure DB). **`/payment/callback` BİLEREK DOKUNULMADI** — public bank-return-URL "her zaman graceful redirect" sanctioned resilience boundary (webhook always-2xx sınıfı), codex-context.md'ye kayıt. Mevcut error code + static mesaj korundu. `work/20260614-backend-payment-typed-catch` (`69115b9f`), push'lu, merge YOK. Codex PASS iter0 (MEDIUM). Shared/errors.md/using dokunulmadı.
  - ✅ **Backend auth/login (Backend-2/6, flow-builder login)** — MUST-ASK gate (Q-onaylı: sanction+DB-typed + GetInt32 hardening). 3 değişiklik: (1) GetInt32→ValueKind+TryGetInt32 guard (non-numeric tenant_id/non-string api_key → mevcut 400 validation, eskiden broad-catch 500); (2) yeni `catch(NpgsqlException)`→DatabaseConnectionFailed (INV-DB-001 reuse)+log+spesifik mesaj (eskiden GeneralUnknown); (3) trailing `catch(Exception)` SANCTIONED auth-resilience boundary olarak KORUNDU (ex.Message log-only, body'de yok), codex-context.md'ye kayıt (`/payment/callback` sınıfı). Happy-path byte-identical. `work/20260614-backend-flowbuilder-login-typed-catch` (`bcc53540`), Codex PASS iter0 (MEDIUM, 12/12 CQ, 4/4 VQ). **MASTER'A MERGE (`30fd9361`) + PROD DEPLOY EDİLDİ** (Q onayı "deploy", single-DLL: Backend.dll publish→stop→upload→start; `.bak-20260614-flowbuilder` rollback prod'da; canlı smoke 4/4 doğru). Branch merge sonrası silindi. Shared/errors.md/using dokunulmadı. **→ Backend grubu (settings/instances + payment + auth/login) 3/3 TAMAM; settings+payment HÂLÂ unmerged (Q kararı).**
  - ✅ **Integrations §7 grubu (Int-2..6)** — Int-2 ikas DI: named HttpClient + singleton-via-IHttpClientFactory+ActivatorUtilities (10s/15s timeout artık uygulanıyor; eskiden AddSingleton<T>() default 100s client'ı shadow ediyordu = worker-wedge riski); Int-3 `pkt6b1-niche-business.sql` GRANT `invekto_app`→`invekto` SOURCE-ONLY (canlı prod grant `information_schema.role_table_grants` ile doğrulandı = 4 tablo zaten `invekto`, migration GEREKMEZ); Int-4 partial-sync detector (ParseOrderList hasNext/count SystemWarn, davranış-koruyan; **tam pagination ERTELENDI** — canlı ikas PaginationInput kontratı bekliyor, Q kararı); Int-5 256KB truncation status/parse'tan ÖNCE coded Fail(INV-IG-016), ham body log YOK; Int-6 SlowEquals→SHA-256 digest+FixedTimeEquals (length-independent, `null→false` guard korundu). `work/20260614-integrations-section7-quickwins` (`100d7a56`), Codex PASS **iter3** (iter0 FAIL=FixedTimeEquals overclaim+null-semantics; iter2 FAIL=truncation ordering + kesik inline diff). Shared/error-code dokunulmadı. Int-1 (COUNT GetInt32) = Batch 1'de kapanmıştı.
  - ✅ **WebChat §7 (WebChat-1..6) DONE + MASTER MERGE (`7d895d47`) + PROD DEPLOY** (2026-06-14 22:38, port 7113 RUNNING, smoke doğrulandı; INV-WC-026 eklendi). Tek deployed+merged audit batch'i (diğerleri unmerged). Kapsam-dışı düşük: WebChat Program.cs ValidateOperatorJwt bare-catch (refute-edilmiş sınıf).
  - ✅ **Automation §7 safe-sweep (16 bulgu, 2 commit) DONE** — Commit A (`f2925866`, Program.cs endpoint'leri: Auto-3/4/5/19/20 broad→NpgsqlException/INV-DB-001 + OCE→499 + sanctioned fire-and-forget/per-item degradation + 3 null-forgiving), Codex PASS iter1. Commit B (`49660d61`, services+repo: Auto-1 redundant tx-catch kaldır + Auto-7 COUNT(*)::int + Auto-9 enrichment OCE+typed + Auto-12 FlowEngineV2 sanctioned broad + Auto-13 ValueKind+TryGetInt32/TryGetDouble + Auto-14/15/16/10 null-forgiving + Auto-18/27 typed), Codex PASS iter1 (iter0 FAIL = ValueKind==Number tek başına yetmez → TryGetInt32). Branch `work/20260614-automation-section7-safe-sweep` (HEAD `f6021a41`), push'lu. codex-context.md'ye 2 yeni sanctioned-broad-catch sınıfı. **Kalan Automation:** Auto-2 SSRF (MUST-ASK), Auto-11/17 real-bug behavior-change, Auto-8/22 TOCTOU.
  - ✅ **CONSOLIDATION (2026-06-15)** — 18 unmerged branch → `work/20260614-audit-consolidation` (yukarıda Güncel). **2 dalga:** Wave-1 kritik (getint32 crash / knowledge-c4 IndexOutOfRange / voiceai `:`-crash+CWE-22 / outbound-jwt 401-broken-since-J2 / failclosed-auth / errorcode-collision) = prod'da-olmayan gerçek bug/security fix'leri; Wave-2 §7 quality sweep'leri (AgentAI×4, automation, integrations, backend settings/payment, knowledge, waa×2, outbound §7). Plan `tracking/audit-consolidation-plan.md` §"Deploy plan" D1-D4 dalga sıralı + per-service smoke. **Gate GREEN.** Neredeyse tüm 11 servis etkilenir = full-platform deploy. **Next decision = Q:** master-merge / deploy (D1→D4 önerilir, tek-shot değil).
  - ✅ **Outbound TAM consolidation'da:** `outbound-section7-quickwins` (`0b8f7c95`) = Scope A (`6cd168eb`, Ob-2/3/5/6/7) **+ üstüne** behavior-change Ob-4/Ob-8 commit'leri (6cd168eb ⊂ 0b8f7c95, ayrı branch YOK) + `outbound-internal-jwt-exclusion-fix` (`fff607fd`) — üçü de integration branch'te.
  - ⏳ **Henüz dokunulmamış audit kalanı:** Automation behavior-change/security (Auto-2 SSRF MUST-ASK, Auto-11/17), Backend low/med, Marketing-1, WAA-8 (ON CONFLICT latent)/WAA-15 (DI dup), Integrations Int-4 full pagination (canlı ikas PaginationInput sonrası).
  - Giriş: iki raporun §4 Quick Wins + §7. ⚠️ Büyük → consolidation sonrası `/clear` önerilir.

## Platform & Gate

**Platform:** 11 .NET 8 mikroservis, tek shared PostgreSQL 16 + pgvector. Prod = services.invekto.com, `C:\Invekto\{Service}\current\`, NSSM. Portlar: Backend 5000, ChatAnalysis 7101, Appointments 7102, Knowledge 7104, AgentAI 7105, Integrations 7106, **Outbound 7107**, Automation 7108, WhatsAppAnalytics 7109, Marketing 7112. FaceAnalysis 7110 + VisualSearch 7111 = Planned. VoiceRuntime + VoiceAI = ayrı NSSM (server-deploy enum'da EKSİK — L-2026-05-31).

**P0-3 Gate (güncel 2026-06-13):** `BulkSend.AllowAllTenants=true` (TÜM tenantlar, bridge route) + `Projects=[5050, 15702882, 100000001]` + `CxapiSend=[5050, 100000001]` (5050 sandbox + Medipol-wappflex). Medipol ack webhook OTOMATİK — FEATURE A `CxapiWebhookReconcileJob` ENABLED/AllowAll prod'da (yabancı URL'lere dokunmaz). Codex = gpt-5.5, LOW dahil her risk seviyesi review.

**Pilot tenant gerçeği:** WapCRM-konfigli canlı tenant'lar = 5050 (vendor sandbox) + Medipol (100000001 wappflex, instance 8784 WABA). cxapi send + delivery-ack zinciri uçtan uca canlı doğrulandı. Medipol = İKİ tenant satırı (mlpcm 15702882 + Medipol 100000001) — firma-bazlı sorguda ikisini de kullan.

## Execution Queue (açık/pending — master: tracking/pilot-launch-roadmap.md)

- **FEAT-INMA-PIPELINE-V2 C4** (refactor-audit sonrası): Set Customer Status write-back — INMA update endpoint + `invekto-{flowRunUuid}` ClientRequestID (`CustomerStatusFlowSuppression.OriginRequestIdPrefix` reuse). **C3 hardening backlog:** bounded-retry + idempotency-table + outbox (AutomaticRetry=0 transient blip'te tek otomasyon kaybı — go-live öncesi). **Prereq:** Medipol (100000001) `webhook_secret` set değil. C3b/C4 canlı smoke: 5050'de INMA panel durum değişikliği → C2→C3a→flow tetik uçtan uca (AC10).
- **⚠️ İZLE:** Backend stderr tarihsel `Body was inferred but the method does not allow inferred body parameters` crash trace (canlı PID sağlıklı, C2 route eklemiyor — ayrı incelenmeli).
- **UP0.3** Tenant lifecycle handler: PENDING (INMA tenant.created event bekliyor). **UP0.5** IInmaSendClient: PENDING (INMA J1/J4 bekliyor).
- **PKT-13 Faz 1** Lead Scoring: PENDING (Marketing). **FEAT-OBI Faz 2:** telefon tek-numara geçmiş arama (Faz 1B DONE+DEPLOYED).
- **FEAT-META-CAPI / META-ADS-INSIGHTS:** DRAFT (Q provision bekliyor: Pixel/token/App Review). META-MARKETING-API: BACKLOG ($50k+/ay gate).
- **Dent Adavista pilot:** BLOCKED (UP0 + FEAT-* sonrası). **Zoho OAuth smoke** (5050 e2e): DEFERRED (INMA creds 401, opsiyonel).

<!-- END_CURRENT_STATE max_lines=300 -->
